# 交接文件 — M7 進度（2026-07-20，額度中斷）

## TL;DR — M7 只做完 Wave 1（DB 層），Wave 2/3 未開始

- **中斷原因**：session 額度上限（台北時間 7pm 重置）。Wave 2/3 的 agent 才跑 5 個 tool uses 就被擋，**沒寫任何檔**。
- **git HEAD**：`3ab1f17 feat: M7 Wave 1 — LINE conversation aggregation + intake-draft DB layer`。前序 M6 `8eeb087`。branch `feat/v2-pilot-r0-m1-m2`，**未 push**。
- 工作區乾淨（Wave 1 已 commit）。
- **進度：M0–M6 Verified + M7 Wave 1（DB）完成。M7 還缺 Wave 2（AI seam/worker/routes）+ Wave 3（UI/E2E）。**

## M7 Wave 1 已交付（DB 層，全綠）

migration `supabase/migrations/202607200008_v2_m7_intake_aggregation.sql` + pgTAP `supabase/tests/13_m7_intake_aggregation.test.sql`（66 tests）+ ADR `docs/adr/0006-intake-aggregation-and-ai-extractor-seam.md`。

驗證：`db reset` 空庫成功、`npm run test:sql` **609**（543+66）、`lint:db`、`typecheck`、`unit 1177` 全綠。

**5 張表**（都 FORCE RLS、org-scoped）：`conversations`（一 sender 一 open conversation partial unique）、`inbound_messages`（append-only，write-once guard trigger，dedup unique on line_message_id）、`message_attachments`（image，private bucket）、`intake_drafts`（origin ai|manual、一 conversation 一 active draft、per-field provenance + confidence）、`intake_extraction_runs`（append-only 稽核）。

**RPCs**（完整簽名見 migration；Wave 2 契約摘要見下）：`ingest_inbound_message`、`create_intake_draft`、`record_extraction_run`、`mark_extraction_succeeded/failed`、`claim_intake_extraction_runs`(SKIP LOCKED)、`confirm_intake_draft`(atomic→service_request source=line、confirm-once)、`dismiss_intake_draft`。

**降級 gate 已在 SQL 保證**：`mark_extraction_failed` 就算 AI 掛也會寫一筆 `origin=manual` 草稿，訊息永不遺失。

## 續跑指引（額度恢復後）

**下一步：發派 M7 Wave 2 + Wave 3。** 現成的 workflow 腳本可直接重跑：

```
Workflow({scriptPath: "…/workflows/scripts/implement-m7-waves-2-3-wf_70214ef8-5a4.js"})
```

（Wave 1 已 commit，不需重跑；此腳本只含 Wave 2/3。腳本裡已嵌入 Wave 1 的真實 RPC 契約與縮短的回報 schema。）

**Wave 1 → Wave 2 契約**（Wave 2 code 依此，或直接讀 migration 202607200008）：
- `ingest_inbound_message(p_webhook_event_id uuid, p_line_message_id text, p_message_type text, p_text_content text, p_raw jsonb, p_line_user_id text, p_customer_line_identity_id uuid=null, p_sent_at timestamptz=null)` → `{conversation_id, message_id, duplicate}`
- `mark_extraction_succeeded(p_org, p_conversation_id, p_extractor_name, p_input_message_ids uuid[], p_confidence, p_summary, p_title, p_fields jsonb, p_missing_fields=null, p_model_version=null, p_output jsonb=null, …)` → 寫 succeeded run + ai draft
- `mark_extraction_failed(p_org, p_conversation_id, p_extractor_name, p_input_message_ids uuid[], p_error_code text, p_latency_ms int=null)` → 寫 failed run + **manual draft**（降級）
- `claim_intake_extraction_runs(p_worker_id text, p_limit int=10, p_now timestamptz=null)` → SKIP LOCKED
- `confirm_intake_draft(target_org uuid, target_draft uuid, target_idempotency_key text=null, p_field_overrides jsonb=null)` → atomic 建 service_request，confirm-once
- `dismiss_intake_draft(target_org uuid, target_draft uuid, p_reason text=null)`

**Wave 2 要建**：`src/server/integrations/ai/{extractor,factory}.ts`（AiExtractor 介面 + FakeAiExtractor + FireworksAiExtractor **deferred stub** + createAiExtractor 依 FIREWORKS_AI_LIVE 切換）、`src/server/integrations/line/content-fetcher.ts`（+ Fake + Real stub）、webhook-handler/gateway 的 message 分支接 ingest、`src/server/intake/gateway.ts`（try/catch 降級呼叫 mark_extraction_failed，不 throw 出邊界）、`src/app/api/v2/internal/workers/intake-extraction/route.ts`、`src/app/api/v2/organizations/[orgId]/intake-drafts/**`（list/detail/confirm/dismiss）、schemas。

**Wave 3 要建**：`pilot-inbox.ts` 加 line draft、inbox badge「待確認 · LINE」、`intake-draft-review.tsx`（不可變原始訊息 + AI 欄位含信心/來源 + 確認建進件）、E2E（模擬 LINE→草稿→確認→進件 + 降級案例）。

**完成後**照慣例：一手回歸（db reset/sql/typecheck/unit/integration/coverage/build/e2e）→ 對抗 review → 修復 → commit → 更新此交接文件。

## deferred seams（M7 完成後仍留最後）

- **真實 Fireworks 呼叫** → `FireworksAiExtractor.extractIntake` body（`FIREWORKS_AI_LIVE` + `FIREWORKS_API_KEY` server-only）。
- **真實 LINE 圖片下載** → `RealLineContentFetcher` body。
- **真實 LINE channel** → M6 既有 deferred（`RealLineMessenger` + `LINE_CHANNEL_*`）。

## 給接手 AI 的踩雷提醒

- Wave 2/3 若 workflow agent 回報「失敗」但實際可能有寫檔——**先 `ls`/`git status` + 讀 transcript + 一手實跑驗證**，不要假設沒做（Wave 1 就是回報失敗但工作全完成）。
- StructuredOutput 大 payload 會觸發 harness schema 驗證器拒收——回報 schema 的欄位描述要短。
- 本機 `dev:local`/`build:local`/`test:e2e:local`；E2E `--workers=1`；改 schema/route 後必跑 `test:integration`。
- 遺留 lint:db 警告：`202607200007` 的 `apply_line_identity_event` 有未讀 `v_row`（M6 遺留，非阻斷，可用 `PERFORM … FOR UPDATE` 取代 `SELECT INTO`，需新 migration）。

## 下一步

- **M7 Wave 2 + 3**（額度恢復後續跑上述腳本）。
- 之後 **M8**（收款、設備履歷、回訪、KPI + production hardening）= 最後一個階段。
