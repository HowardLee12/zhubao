openapi: 3.1.0
info:
  title: Renoly v2 API
  version: 0.1.0
  description: |
    Machine-readable contract for the clean-slate Renoly v2 API.
    The normative prose remains docs/api-spec.md. Employee operations always
    verify the path organization, active membership, role and resource tenant.
    Cross-tenant resources are returned as 404 to avoid existence disclosure.
  contact:
    name: Renoly engineering
servers:
  - url: /api/v2
    description: Same-origin application API

x-employee-read-security: &employeeReadSecurity
  - employeeSession: []
x-employee-write-security: &employeeWriteSecurity
  - employeeSession: []
    csrfToken: []
x-line-webhook-security: &lineWebhookSecurity
  - lineSignature: []
x-standard-errors: &standardErrors
  "400":
    $ref: "#/components/responses/BadRequest"
  "401":
    $ref: "#/components/responses/Unauthorized"
  "403":
    $ref: "#/components/responses/Forbidden"
  "404":
    $ref: "#/components/responses/NotFound"
  "409":
    $ref: "#/components/responses/Conflict"
  "412":
    $ref: "#/components/responses/PreconditionFailed"
  "422":
    $ref: "#/components/responses/UnprocessableEntity"
  "428":
    $ref: "#/components/responses/PreconditionRequired"
  "429":
    $ref: "#/components/responses/RateLimited"
  "500":
    $ref: "#/components/responses/InternalError"

security: *employeeReadSecurity

tags:
  - name: Health
  - name: Session
  - name: Organizations
  - name: Customers
  - name: Service requests
  - name: Public intake
  - name: Projects
  - name: Work orders
  - name: Checklists
  - name: Photos
  - name: Events
  - name: Service catalog
  - name: Quotes
  - name: Change orders
  - name: Payments
  - name: Maintenance
  - name: Notifications
  - name: LINE
  - name: Dashboard

paths:
  /health:
    get:
      tags: [Health]
      operationId: getHealth
      summary: Process liveness and deployment health
      description: Does not expose credentials, dependency URLs or tenant data.
      security: []
      responses:
        <<: *standardErrors
        "200":
          description: Service is accepting requests.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthEnvelope"

  /session:
    get:
      tags: [Session]
      operationId: getSession
      summary: Get the authenticated user and active memberships
      responses:
        <<: *standardErrors
        "200":
          description: Current session without provider tokens.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionEnvelope"

  /organizations:
    post:
      tags: [Organizations]
      operationId: createOrganization
      summary: Create an organization and owner membership atomically
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOrganizationRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Organization and owner membership created.
          headers:
            Location:
              $ref: "#/components/headers/Location"
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationCreatedEnvelope"

  /organizations/{orgId}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Organizations]
      operationId: getOrganization
      summary: Get an organization summary
      responses:
        <<: *standardErrors
        "200":
          description: Organization summary.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationEnvelope"
    patch:
      tags: [Organizations]
      operationId: updateOrganization
      summary: Update low-risk organization settings
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateOrganizationRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Updated organization.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationEnvelope"

  /organizations/{orgId}/settings:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Organizations]
      operationId: getPilotOrganizationSettings
      summary: Get the pilot organization intake settings
      description: |
        Pilot slice settings backing the public intake form. Returns the owner-
        editable name, headline and privacy notice with an optimistic-lock
        version. Implemented by the get_pilot_organization_settings RPC.
      responses:
        <<: *standardErrors
        "200":
          description: Pilot organization settings.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotOrganizationSettingsEnvelope"
    patch:
      tags: [Organizations]
      operationId: updatePilotOrganizationSettings
      summary: Update pilot organization intake settings under optimistic locking
      description: |
        Only the pilot owner or admin may edit. The client must send the current
        lockVersion; a stale version is rejected with 409. Implemented by the
        update_pilot_organization_settings RPC.
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePilotOrganizationSettingsRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Updated pilot organization settings.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotOrganizationSettingsEnvelope"

  /organizations/{orgId}/public-intake-link/actions/rotate:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    post:
      tags: [Organizations, Public intake]
      operationId: rotatePilotPublicIntakeLink
      summary: Rotate the pilot public intake link and return the new URL once
      description: |
        Mints a new public intake token, stores only its hash, and returns the
        raw share URL exactly once. The previous link stops working. Only the
        pilot owner or admin may rotate. Implemented by the
        rotate_pilot_intake_token RPC.
      security: *employeeWriteSecurity
      responses:
        <<: *standardErrors
        "200":
          description: New public intake share URL, returned once.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotPublicIntakeLinkEnvelope"

  /organizations/{orgId}/members:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Organizations]
      operationId: listPilotAssignableMembers
      summary: List active operational members for the pilot assignment picker
      description: |
        Manager-only pilot projection. Returns no email, phone, user id or
        organization id; accountant/viewer and inactive memberships are omitted.
      responses:
        <<: *standardErrors
        "200":
          description: Active owner/admin/dispatcher/technician memberships.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotAssignableMemberListEnvelope"

  /organizations/{orgId}/memberships:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Organizations]
      operationId: listMemberships
      summary: List organization memberships
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        <<: *standardErrors
        "200":
          description: Membership page; technician and viewer DTOs are redacted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MembershipListEnvelope"
    post:
      tags: [Organizations]
      operationId: inviteMembership
      summary: Invite an organization member
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InviteMembershipRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Membership invitation created.
          headers:
            Location:
              $ref: "#/components/headers/Location"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MembershipEnvelope"

  /organizations/{orgId}/memberships/{id}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    patch:
      tags: [Organizations]
      operationId: updateMembership
      summary: Update membership display data
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateMembershipRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Membership updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MembershipEnvelope"

  /organizations/{orgId}/memberships/{id}/actions/change-role:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    post:
      tags: [Organizations]
      operationId: changeMembershipRole
      summary: Change a member role without removing the last owner
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChangeRoleRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Role changed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MembershipEnvelope"

  /organizations/{orgId}/memberships/{id}/actions/suspend:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    post:
      tags: [Organizations]
      operationId: suspendMembership
      summary: Suspend a membership
      security: *employeeWriteSecurity
      responses:
        <<: *standardErrors
        "200":
          description: Membership suspended.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MembershipEnvelope"

  /organizations/{orgId}/memberships/{id}/actions/restore:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    post:
      tags: [Organizations]
      operationId: restoreMembership
      summary: Restore a suspended membership
      security: *employeeWriteSecurity
      responses:
        <<: *standardErrors
        "200":
          description: Membership restored.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MembershipEnvelope"

  /organizations/{orgId}/customers:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Customers]
      operationId: listCustomers
      summary: List customers for the pilot triage workspace
      parameters:
        - name: q
          in: query
          schema:
            type: string
            minLength: 1
            maxLength: 120
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 20
      responses:
        <<: *standardErrors
        "200":
          description: Customer confirmation rows; no internal columns or cursor metadata.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotCustomerListEnvelope"
    post:
      tags: [Customers]
      operationId: createCustomer
      summary: Create a customer
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PilotCreateCustomerRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Customer created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotCustomerEnvelope"

  /organizations/{orgId}/customers/similar:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Customers]
      operationId: findSimilarCustomers
      summary: Find similar customers for triage duplicate hints
      description: >-
        Tenant-scoped hint search over phone and name used while triaging a
        request. At least one of phone or name must be provided. Returns
        hint-only rows; it never merges customers.
      parameters:
        - name: phone
          in: query
          schema:
            $ref: "#/components/schemas/E164Phone"
        - name: name
          in: query
          schema:
            type: string
            minLength: 1
            maxLength: 200
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 25
            default: 5
      responses:
        <<: *standardErrors
        "200":
          description: Similar customer hints.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SimilarCustomerListEnvelope"

  /organizations/{orgId}/customers/{customerId}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/CustomerId"
    get:
      tags: [Customers]
      operationId: getCustomer
      summary: Get a customer
      parameters:
        - name: include
          in: query
          schema:
            type: string
            enum: [defaultLocation]
      responses:
        <<: *standardErrors
        "200":
          description: Customer; assigned technicians receive a constrained DTO.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerEnvelope"
    patch:
      tags: [Customers]
      operationId: updateCustomer
      summary: Update a customer
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateCustomerRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Customer updated.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerEnvelope"
    delete:
      tags: [Customers]
      operationId: deleteCustomer
      summary: Soft-delete a customer
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      responses:
        <<: *standardErrors
        "204":
          $ref: "#/components/responses/NoContent"

  /organizations/{orgId}/customers/{customerId}/locations:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/CustomerId"
    get:
      tags: [Customers]
      operationId: listCustomerLocations
      summary: List customer locations
      responses:
        <<: *standardErrors
        "200":
          description: Customer locations.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotCustomerLocationListEnvelope"
    post:
      tags: [Customers]
      operationId: createCustomerLocation
      summary: Create a customer service location
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateLocationRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Location created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LocationEnvelope"

  /organizations/{orgId}/customers/{customerId}/assets:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/CustomerId"
    get:
      tags: [Customers]
      operationId: listPilotCustomerAssets
      summary: List a customer's assets for the pilot triage workspace
      responses:
        <<: *standardErrors
        "200":
          description: Customer assets projected by the authenticated pilot RPC.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotCustomerAssetListEnvelope"

  /organizations/{orgId}/locations/{locationId}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/LocationId"
    get:
      tags: [Customers]
      operationId: getLocation
      summary: Get a service location
      responses:
        <<: *standardErrors
        "200":
          description: Service location.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LocationEnvelope"
    patch:
      tags: [Customers]
      operationId: updateLocation
      summary: Update a service location
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLocationRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Location updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LocationEnvelope"
    delete:
      tags: [Customers]
      operationId: deleteLocation
      summary: Soft-delete a location when no active work order depends on it
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      responses:
        <<: *standardErrors
        "204":
          $ref: "#/components/responses/NoContent"

  /organizations/{orgId}/locations/{locationId}/assets:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/LocationId"
    get:
      tags: [Customers]
      operationId: listLocationAssets
      summary: List assets at a location
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/AssetStatus"
        - name: assetType
          in: query
          schema:
            type: string
            minLength: 1
            maxLength: 80
      responses:
        <<: *standardErrors
        "200":
          description: Asset page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetListEnvelope"
    post:
      tags: [Customers]
      operationId: createAsset
      summary: Create an asset at a customer location
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateAssetRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Asset created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetEnvelope"

  /organizations/{orgId}/assets/{assetId}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/AssetId"
    get:
      tags: [Customers]
      operationId: getAsset
      summary: Get an asset
      responses:
        <<: *standardErrors
        "200":
          description: Asset.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetEnvelope"
    patch:
      tags: [Customers]
      operationId: updateAsset
      summary: Update an asset
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateAssetRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Asset updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetEnvelope"
    delete:
      tags: [Customers]
      operationId: retireAsset
      summary: Retire and soft-delete an asset while retaining history
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      responses:
        <<: *standardErrors
        "204":
          $ref: "#/components/responses/NoContent"

  /organizations/{orgId}/assets/{assetId}/history:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/AssetId"
    get:
      tags: [Customers]
      operationId: getAssetHistory
      summary: Get merged work-order, photo and event history for an asset
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        <<: *standardErrors
        "200":
          description: Asset history page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EventListEnvelope"

  /organizations/{orgId}/service-requests:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Service requests]
      operationId: listServiceRequests
      summary: List pilot inbox service requests
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/ServiceRequestStatus"
      responses:
        <<: *standardErrors
        "200":
          description: Service request page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotInboxListEnvelope"
    post:
      tags: [Service requests]
      operationId: createServiceRequest
      summary: Create a staff-entered service request
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IdempotencyKeyOptional"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateServiceRequestRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Service request created.
          headers:
            Location:
              $ref: "#/components/headers/Location"
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceRequestEnvelope"

  /organizations/{orgId}/service-requests/{id}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    get:
      tags: [Service requests]
      operationId: getServiceRequest
      summary: Get a service request
      responses:
        <<: *standardErrors
        "200":
          description: Service request.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotServiceRequestDetailEnvelope"
    patch:
      tags: [Service requests]
      operationId: updateServiceRequest
      summary: Update service request content without changing status
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PilotUpdateServiceRequestSummaryRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Service request updated.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotServiceRequestDetailEnvelope"

  /organizations/{orgId}/service-requests/{id}/actions/triage:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    post:
      tags: [Service requests]
      operationId: triageServiceRequest
      summary: Link request context and move new to triaged
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TriageServiceRequestRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Triaged request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotServiceRequestActionEnvelope"

  /organizations/{orgId}/service-requests/{id}/actions/start-quoting:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    post:
      tags: [Service requests]
      operationId: startServiceRequestQuoting
      summary: Move a triaged request into quoting
      security: *employeeWriteSecurity
      responses:
        <<: *standardErrors
        "200":
          description: Request now quoting.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotServiceRequestActionEnvelope"

  /organizations/{orgId}/service-requests/{id}/actions/mark-quoted:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    post:
      tags: [Service requests]
      operationId: markServiceRequestQuoted
      summary: Mark request quoted after a quote version was sent
      security: *employeeWriteSecurity
      responses:
        <<: *standardErrors
        "200":
          description: Request marked quoted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotServiceRequestActionEnvelope"

  /organizations/{orgId}/service-requests/{id}/actions/convert:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Service requests]
      operationId: convertServiceRequest
      summary: Convert request atomically into a work order or project
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConvertServiceRequestRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Conversion resources created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotConversionEnvelope"

  /organizations/{orgId}/service-requests/{id}/actions/decline:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    post:
      tags: [Service requests]
      operationId: declineServiceRequest
      summary: Decline a service request with a reason
      security: *employeeWriteSecurity
      requestBody:
        $ref: "#/components/requestBodies/ReasonRequired"
      responses:
        <<: *standardErrors
        "200":
          description: Request declined.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotServiceRequestActionEnvelope"

  /organizations/{orgId}/service-requests/{id}/actions/cancel:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    post:
      tags: [Service requests]
      operationId: cancelServiceRequest
      summary: Cancel a service request with a reason
      security: *employeeWriteSecurity
      requestBody:
        $ref: "#/components/requestBodies/ReasonRequired"
      responses:
        <<: *standardErrors
        "200":
          description: Request cancelled.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotServiceRequestActionEnvelope"

  /organizations/{orgId}/service-requests/{id}/events:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    get:
      tags: [Events]
      operationId: listServiceRequestEvents
      summary: List append-only service request events
      parameters:
        - name: afterSequence
          in: query
          schema:
            type: integer
            minimum: 1
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        <<: *standardErrors
        "200":
          description: Event page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotServiceRequestEventListEnvelope"

  /organizations/{orgId}/service-requests/{id}/photos:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    get:
      tags: [Service requests]
      operationId: listPilotServiceRequestPhotos
      summary: List private intake photos with short-lived signed URLs
      responses:
        <<: *standardErrors
        "200":
          description: Signed photo projections; storage paths are never returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotServiceRequestPhotoListEnvelope"

  /public/intake/{token}:
    parameters:
      - $ref: "#/components/parameters/PublicToken"
    get:
      tags: [Public intake]
      operationId: getPublicIntakeForm
      summary: Get merchant branding and intake form schema
      security: []
      responses:
        <<: *standardErrors
        "200":
          description: Public intake configuration without internal data.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicIntakeEnvelope"

  /public/intake/{token}/service-requests:
    parameters:
      - $ref: "#/components/parameters/PublicToken"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Public intake]
      operationId: createPublicServiceRequest
      summary: Submit a public service request
      description: Subject to token/IP rate limits, honeypot and adaptive CAPTCHA.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PublicServiceRequestRequest"
      responses:
        <<: *standardErrors
        "202":
          description: Request accepted without exposing internal UUIDs.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicServiceRequestAcceptedEnvelope"

  /public/intake/{token}/photo-uploads:
    parameters:
      - $ref: "#/components/parameters/PublicToken"
    post:
      tags: [Public intake, Photos]
      operationId: createPublicIntakePhotoUpload
      summary: Create a pending public intake photo and signed upload URL
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PublicPhotoUploadRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Pending photo and short-lived signed upload instruction.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PhotoUploadEnvelope"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"

  /public/intake/{token}/photos/{photoId}/complete:
    parameters:
      - $ref: "#/components/parameters/PublicToken"
      - $ref: "#/components/parameters/PhotoId"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Public intake, Photos]
      operationId: completePublicIntakePhoto
      summary: Verify and finalize a public intake photo upload
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PublicCompletePhotoUploadRequest"
      responses:
        <<: *standardErrors
        "202":
          description: Photo verified against its declared checksum and marked ready.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicPhotoReadyEnvelope"

  /organizations/{orgId}/projects:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Projects]
      operationId: listProjects
      summary: List projects
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/SearchQuery"
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/ProjectStatus"
        - name: customerId
          in: query
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: ownerMemberId
          in: query
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: startFrom
          in: query
          schema:
            $ref: "#/components/schemas/BusinessDate"
        - name: startTo
          in: query
          schema:
            $ref: "#/components/schemas/BusinessDate"
      responses:
        <<: *standardErrors
        "200":
          description: Project page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectListEnvelope"
    post:
      tags: [Projects]
      operationId: createProject
      summary: Create a project
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IdempotencyKeyOptional"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProjectRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Project created.
          headers:
            Location:
              $ref: "#/components/headers/Location"
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectEnvelope"

  /organizations/{orgId}/projects/{id}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    get:
      tags: [Projects]
      operationId: getProject
      summary: Get a project
      responses:
        <<: *standardErrors
        "200":
          description: Project.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectEnvelope"
    patch:
      tags: [Projects]
      operationId: updateProject
      summary: Update project content or planned dates without changing state
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateProjectRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Project updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectEnvelope"

  /organizations/{orgId}/projects/{id}/actions/activate:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    post:
      tags: [Projects]
      operationId: activateProject
      summary: Activate a draft project
      security: *employeeWriteSecurity
      responses:
        <<: *standardErrors
        "200":
          description: Active project.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectEnvelope"

  /organizations/{orgId}/projects/{id}/actions/hold:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    post:
      tags: [Projects]
      operationId: holdProject
      summary: Put an active project on hold
      security: *employeeWriteSecurity
      requestBody:
        $ref: "#/components/requestBodies/ReasonRequired"
      responses:
        <<: *standardErrors
        "200":
          description: Project on hold.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectEnvelope"

  /organizations/{orgId}/projects/{id}/actions/resume:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    post:
      tags: [Projects]
      operationId: resumeProject
      summary: Resume an on-hold project
      security: *employeeWriteSecurity
      responses:
        <<: *standardErrors
        "200":
          description: Active project.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectEnvelope"

  /organizations/{orgId}/projects/{id}/actions/complete:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    post:
      tags: [Projects]
      operationId: completeProject
      summary: Complete a project after all non-cancelled work orders complete
      security: *employeeWriteSecurity
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompleteProjectRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Completed project.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectEnvelope"

  /organizations/{orgId}/projects/{id}/actions/cancel:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    post:
      tags: [Projects]
      operationId: cancelProject
      summary: Cancel a project
      security: *employeeWriteSecurity
      requestBody:
        $ref: "#/components/requestBodies/ReasonRequired"
      responses:
        <<: *standardErrors
        "200":
          description: Cancelled project.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectEnvelope"

  /organizations/{orgId}/projects/{id}/summary:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    get:
      tags: [Projects]
      operationId: getProjectSummary
      summary: Get a bounded project aggregate summary
      responses:
        <<: *standardErrors
        "200":
          description: Project summary without N+1 expansion.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectSummaryEnvelope"

  /organizations/{orgId}/work-orders:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Work orders]
      operationId: listWorkOrders
      summary: List work orders visible to the current member
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/SearchQuery"
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/WorkOrderStatus"
        - name: assigneeId
          in: query
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: projectId
          in: query
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: customerId
          in: query
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: assetId
          in: query
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: scheduledFrom
          in: query
          schema:
            $ref: "#/components/schemas/Timestamp"
        - name: scheduledTo
          in: query
          schema:
            $ref: "#/components/schemas/Timestamp"
        - name: priority
          in: query
          schema:
            $ref: "#/components/schemas/Priority"
      responses:
        <<: *standardErrors
        "200":
          description: Work-order page; technicians only receive assigned work.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkOrderListEnvelope"
    post:
      tags: [Work orders]
      operationId: createWorkOrder
      summary: Create a work order
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IdempotencyKeyOptional"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWorkOrderRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Work order created.
          headers:
            Location:
              $ref: "#/components/headers/Location"
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkOrderDetailEnvelope"

  /organizations/{orgId}/work-orders/{id}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    get:
      tags: [Work orders]
      operationId: getWorkOrder
      summary: Get a work order
      responses:
        <<: *standardErrors
        "200":
          description: Work order, redacted according to role and assignment.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkOrderDetailEnvelope"
    patch:
      tags: [Work orders]
      operationId: updateWorkOrder
      summary: Update allowed work-order content without status or scheduling
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateWorkOrderRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Work order updated.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkOrderDetailEnvelope"

  /organizations/{orgId}/work-orders/{id}/actions/schedule:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    post:
      tags: [Work orders]
      operationId: scheduleWorkOrder
      summary: Schedule and assign a work order
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScheduleWorkOrderRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Scheduled work order.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkOrderMutationEnvelope"

  /organizations/{orgId}/work-orders/{id}/actions/transition:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Work orders]
      operationId: transitionWorkOrder
      summary: Apply a server-decided work-order transition
      description: Client sends an action, never a target status.
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TransitionWorkOrderRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Transitioned work order.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkOrderMutationEnvelope"

  /organizations/{orgId}/work-orders/{id}/assignments:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    get:
      tags: [Work orders]
      operationId: listWorkOrderAssignments
      summary: List assignments for a work order
      responses:
        <<: *standardErrors
        "200":
          description: Assignment list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssignmentListEnvelope"
    post:
      tags: [Work orders]
      operationId: createWorkOrderAssignment
      summary: Assign an active organization member
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateAssignmentRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Assignment created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssignmentEnvelope"

  /organizations/{orgId}/assignments/{assignmentId}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/AssignmentId"
    patch:
      tags: [Work orders]
      operationId: updateAssignment
      summary: Update assignment duty
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateAssignmentRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Assignment updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssignmentEnvelope"
    delete:
      tags: [Work orders]
      operationId: cancelAssignment
      summary: Cancel an assignment while retaining its history
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CancelAssignmentRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Assignment cancelled; the cancelled assignment is returned.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssignmentEnvelope"

  /organizations/{orgId}/assignments/{assignmentId}/actions/respond:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/AssignmentId"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Work orders]
      operationId: respondToAssignment
      summary: Accept or decline the current technician's own assignment
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RespondAssignmentRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Assignment response recorded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssignmentEnvelope"

  /organizations/{orgId}/schedule:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Work orders]
      operationId: getSchedule
      summary: Get schedule entries over a required window of at most 31 days
      parameters:
        - name: from
          in: query
          required: true
          schema:
            $ref: "#/components/schemas/Timestamp"
        - name: to
          in: query
          required: true
          schema:
            $ref: "#/components/schemas/Timestamp"
        - name: assigneeId
          in: query
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        <<: *standardErrors
        "200":
          description: Schedule entries visible to the current member.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkOrderListEnvelope"

  /organizations/{orgId}/schedule/conflict-check:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    post:
      tags: [Work orders]
      operationId: checkScheduleConflicts
      summary: Check up to 50 candidate work-order time windows
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CheckScheduleConflictsRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Candidate conflicts.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScheduleConflictEnvelope"

  /organizations/{orgId}/checklist-templates:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Checklists]
      operationId: listChecklistTemplates
      summary: List checklist templates
      responses:
        <<: *standardErrors
        "200":
          description: Checklist templates.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChecklistTemplateListEnvelope"
    post:
      tags: [Checklists]
      operationId: createChecklistTemplate
      summary: Create a checklist template
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateChecklistTemplateRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Checklist template created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChecklistTemplateEnvelope"

  /organizations/{orgId}/checklist-templates/{id}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    get:
      tags: [Checklists]
      operationId: getChecklistTemplate
      summary: Get a checklist template
      responses:
        <<: *standardErrors
        "200":
          description: Checklist template.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChecklistTemplateEnvelope"
    patch:
      tags: [Checklists]
      operationId: updateChecklistTemplate
      summary: Update a template without changing existing snapshots
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateChecklistTemplateRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Checklist template updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChecklistTemplateEnvelope"

  /organizations/{orgId}/work-orders/{id}/checklists:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    get:
      tags: [Checklists]
      operationId: listWorkOrderChecklists
      summary: List checklist snapshots for a work order
      responses:
        <<: *standardErrors
        "200":
          description: Work-order checklists.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkOrderChecklistListEnvelope"
    post:
      tags: [Checklists]
      operationId: createWorkOrderChecklist
      summary: Snapshot a template or inline checklist onto a work order
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWorkOrderChecklistRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Work-order checklist created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkOrderChecklistEnvelope"

  /organizations/{orgId}/work-order-checklists/{id}/items/{itemId}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/ItemId"
      - $ref: "#/components/parameters/IfMatch"
    patch:
      tags: [Checklists]
      operationId: updateWorkOrderChecklistItem
      summary: Update a checklist response under parent optimistic locking
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateChecklistItemResponseRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Checklist item response updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkOrderChecklistEnvelope"

  /organizations/{orgId}/work-order-checklists/{id}/actions/complete:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Checklists]
      operationId: completeWorkOrderChecklist
      summary: Validate required responses/evidence and complete a checklist
      security: *employeeWriteSecurity
      responses:
        <<: *standardErrors
        "200":
          description: Completed checklist.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkOrderChecklistEnvelope"

  /organizations/{orgId}/work-orders/{id}/photo-uploads:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    post:
      tags: [Photos]
      operationId: createWorkOrderPhotoUpload
      summary: Create a pending private work-order photo and signed PUT
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PhotoUploadRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Pending photo and short-lived upload instruction.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PhotoUploadEnvelope"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"

  /organizations/{orgId}/service-requests/{id}/photo-uploads:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    post:
      tags: [Photos]
      operationId: createServiceRequestPhotoUpload
      summary: Create a pending private service-request photo and signed PUT
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PhotoUploadRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Pending photo and short-lived upload instruction.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PhotoUploadEnvelope"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"

  /organizations/{orgId}/photos/{photoId}/complete:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/PhotoId"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Photos]
      operationId: completePhotoUpload
      summary: Verify object metadata and finalize a private photo upload
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompletePhotoUploadRequest"
      responses:
        <<: *standardErrors
        "202":
          description: Photo accepted for asynchronous processing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PhotoEnvelope"

  /organizations/{orgId}/photos/{photoId}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/PhotoId"
    get:
      tags: [Photos]
      operationId: getPhoto
      summary: Get authorized photo metadata and a short-lived read URL
      responses:
        <<: *standardErrors
        "200":
          description: Photo metadata with optional short-lived read URL.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PhotoEnvelope"
    patch:
      tags: [Photos]
      operationId: updatePhoto
      summary: Update photo caption
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePhotoRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Photo metadata updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PhotoEnvelope"
    delete:
      tags: [Photos]
      operationId: deletePhoto
      summary: Soft-delete a photo when evidence rules permit
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      responses:
        <<: *standardErrors
        "204":
          $ref: "#/components/responses/NoContent"

  /organizations/{orgId}/{resource}/{id}/events:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - name: resource
        in: path
        required: true
        schema:
          type: string
          enum:
            - service-requests
            - projects
            - work-orders
            - quotes
            - change-orders
            - payment-milestones
            - maintenance-plans
            - line-channels
      - $ref: "#/components/parameters/ResourceId"
    get:
      tags: [Events]
      operationId: listAggregateEvents
      summary: List append-only events for an authorized aggregate
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        <<: *standardErrors
        "200":
          description: Event page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EventListEnvelope"

  /organizations/{orgId}/service-catalogs:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Service catalog]
      operationId: listServiceCatalogs
      summary: List service catalogs
      responses:
        <<: *standardErrors
        "200":
          description: Service catalogs.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceCatalogListEnvelope"
    post:
      tags: [Service catalog]
      operationId: createServiceCatalog
      summary: Create a service catalog
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateServiceCatalogRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Catalog created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceCatalogEnvelope"

  /organizations/{orgId}/service-catalogs/{id}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    get:
      tags: [Service catalog]
      operationId: getServiceCatalog
      summary: Get a service catalog
      responses:
        <<: *standardErrors
        "200":
          description: Catalog; technician DTO excludes costs.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceCatalogEnvelope"
    patch:
      tags: [Service catalog]
      operationId: updateServiceCatalog
      summary: Update a service catalog
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateServiceCatalogRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Catalog updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceCatalogEnvelope"

  /organizations/{orgId}/service-catalogs/{id}/items:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    get:
      tags: [Service catalog]
      operationId: listServiceCatalogItems
      summary: List catalog items
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/SearchQuery"
        - name: active
          in: query
          schema:
            type: boolean
        - name: category
          in: query
          schema:
            type: string
            maxLength: 120
      responses:
        <<: *standardErrors
        "200":
          description: Catalog items; technician DTO excludes costs.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceCatalogItemListEnvelope"
    post:
      tags: [Service catalog]
      operationId: createServiceCatalogItem
      summary: Create a service catalog item
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateServiceCatalogItemRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Catalog item created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceCatalogItemEnvelope"

  /organizations/{orgId}/service-catalog-items/{id}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    get:
      tags: [Service catalog]
      operationId: getServiceCatalogItem
      summary: Get a service catalog item
      responses:
        <<: *standardErrors
        "200":
          description: Catalog item with role-based cost redaction.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceCatalogItemEnvelope"
    patch:
      tags: [Service catalog]
      operationId: updateServiceCatalogItem
      summary: Update a service catalog item
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateServiceCatalogItemRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Catalog item updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceCatalogItemEnvelope"
    delete:
      tags: [Service catalog]
      operationId: deactivateServiceCatalogItem
      summary: Deactivate a catalog item without altering historical snapshots
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      responses:
        <<: *standardErrors
        "204":
          $ref: "#/components/responses/NoContent"

  /organizations/{orgId}/service-catalogs/{id}/items:bulk-upsert:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    post:
      tags: [Service catalog]
      operationId: bulkUpsertServiceCatalogItems
      summary: Atomically upsert at most 200 catalog items
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkUpsertCatalogItemsRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Bulk result.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkUpsertCatalogItemsEnvelope"

  /organizations/{orgId}/quotes:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Quotes]
      operationId: listQuotes
      summary: List quotes
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/SearchQuery"
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/QuoteStatus"
        - name: customerId
          in: query
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: projectId
          in: query
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: updatedFrom
          in: query
          schema:
            $ref: "#/components/schemas/Timestamp"
      responses:
        <<: *standardErrors
        "200":
          description: Quote page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuoteListEnvelope"
    post:
      tags: [Quotes]
      operationId: createQuote
      summary: Atomically create a quote and its first server-calculated draft version
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateQuoteRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Quote and first version created; server calculated totals.
          headers:
            Location:
              $ref: "#/components/headers/Location"
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotQuoteWorkspaceEnvelope"

  /organizations/{orgId}/quotes/{id}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    get:
      tags: [Quotes]
      operationId: getQuote
      summary: Get the current staff quote workspace
      responses:
        <<: *standardErrors
        "200":
          description: Quote with allowlisted version expansion.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotQuoteWorkspaceEnvelope"
    patch:
      tags: [Quotes]
      operationId: updateQuote
      summary: Update allowed quote aggregate fields without editing a sent version
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateQuoteRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Quote updated.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuoteEnvelope"

  /organizations/{orgId}/quotes/{id}/versions:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Quotes]
      operationId: createQuoteVersion
      summary: Create a blank or cloned draft quote version
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateQuoteVersionRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Draft version created without changing the active public version.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotQuoteWorkspaceEnvelope"

  /organizations/{orgId}/quote-versions/{versionId}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/VersionId"
    get:
      tags: [Quotes]
      operationId: getQuoteVersion
      summary: Get a quote version
      responses:
        <<: *standardErrors
        "200":
          description: Quote version with internal fields allowed by role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuoteVersionEnvelope"
    patch:
      tags: [Quotes]
      operationId: updateQuoteVersion
      summary: Update an editable draft and invalidate any previous approval
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QuoteVersionInput"
      responses:
        <<: *standardErrors
        "200":
          description: Draft version updated with approval reset as required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotQuoteWorkspaceEnvelope"

  /organizations/{orgId}/quote-versions/{versionId}/items:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/VersionId"
      - $ref: "#/components/parameters/IfMatch"
    put:
      tags: [Quotes]
      operationId: replaceQuoteVersionItems
      summary: Atomically replace at most 300 items on an editable draft
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReplaceQuoteItemsRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Draft items replaced and totals recalculated by the server.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuoteVersionEnvelope"

  /organizations/{orgId}/quote-versions/{versionId}/actions/submit-approval:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/VersionId"
      - $ref: "#/components/parameters/IfMatch"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Quotes]
      operationId: submitQuoteVersionApproval
      summary: Submit a valid draft for owner approval
      security: *employeeWriteSecurity
      responses:
        <<: *standardErrors
        "200":
          description: Approval is pending; version content is frozen.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuoteVersionEnvelope"

  /organizations/{orgId}/quote-versions/{versionId}/actions/approve:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/VersionId"
      - $ref: "#/components/parameters/IfMatch"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Quotes]
      operationId: approveQuoteVersion
      summary: Owner/admin approval of the exact immutable draft content
      security: *employeeWriteSecurity
      responses:
        <<: *standardErrors
        "200":
          description: Quote version approved.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuoteVersionEnvelope"

  /organizations/{orgId}/quote-versions/{versionId}/actions/reject-approval:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/VersionId"
      - $ref: "#/components/parameters/IfMatch"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Quotes]
      operationId: rejectQuoteVersionApproval
      summary: Return a pending approval with a required reason
      security: *employeeWriteSecurity
      requestBody:
        $ref: "#/components/requestBodies/ReasonRequired"
      responses:
        <<: *standardErrors
        "200":
          description: Approval rejected and draft editable again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuoteVersionEnvelope"

  /organizations/{orgId}/quotes/{id}/actions/send:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Quotes]
      operationId: sendQuote
      summary: Owner/admin approves and locks the exact quote version, returning a replay-stable public URL
      description: LINE delivery is M6; M4 returns a capability URL for the owner to copy into the existing customer conversation.
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendQuoteRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Quote sent and one-time raw customer URL returned; only its hash is stored.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotQuoteLinkEnvelope"

  /organizations/{orgId}/quotes/{id}/actions/rotate-public-link:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Quotes]
      operationId: rotateQuotePublicLink
      summary: Owner/admin revokes prior quote links and returns an idempotently replayable new URL
      security: *employeeWriteSecurity
      responses:
        <<: *standardErrors
        "200":
          description: Previous links revoked and a new raw capability URL returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotQuoteLinkEnvelope"

  /organizations/{orgId}/service-requests/{id}/quote:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    get:
      tags: [Quotes]
      operationId: getQuoteByServiceRequest
      summary: Reopen the single quote aggregate attached to a service request
      responses:
        <<: *standardErrors
        "200":
          description: Current staff quote workspace for the request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PilotQuoteWorkspaceEnvelope"

  /organizations/{orgId}/quotes/{id}/actions/cancel:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    post:
      tags: [Quotes]
      operationId: cancelQuote
      summary: Cancel a quote with a reason
      security: *employeeWriteSecurity
      requestBody:
        $ref: "#/components/requestBodies/ReasonRequired"
      responses:
        <<: *standardErrors
        "200":
          description: Quote cancelled.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuoteEnvelope"

  /public/quotes/current:
    get:
      tags: [Quotes]
      operationId: getPublicQuote
      summary: Get the sanitized active public quote version
      security:
        - publicCapabilityBearer: []
      responses:
        <<: *standardErrors
        "200":
          description: Customer-visible quote without costs or internal fields.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicQuoteEnvelope"

  /public/quotes/current/responses:
    parameters:
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Quotes]
      operationId: respondToPublicQuote
      summary: Accept or reject the current active quote version
      security:
        - publicCapabilityBearer: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PublicQuoteResponseRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Response recorded once for the active version.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicDecisionEnvelope"

  /organizations/{orgId}/change-orders:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Change orders]
      operationId: listChangeOrders
      summary: List project change orders
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
        - name: projectId
          in: query
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/ChangeOrderStatus"
        - name: kind
          in: query
          schema:
            $ref: "#/components/schemas/ChangeOrderKind"
      responses:
        <<: *standardErrors
        "200":
          description: Change-order page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChangeOrderListEnvelope"
    post:
      tags: [Change orders]
      operationId: createChangeOrder
      summary: Atomically create a draft change order and items
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateChangeOrderRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Draft change order created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChangeOrderEnvelope"

  /organizations/{orgId}/change-orders/{id}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    get:
      tags: [Change orders]
      operationId: getChangeOrder
      summary: Get a change order
      responses:
        <<: *standardErrors
        "200":
          description: Change order.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChangeOrderEnvelope"
    patch:
      tags: [Change orders]
      operationId: updateChangeOrder
      summary: Update a draft change order only
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateChangeOrderRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Draft change order updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChangeOrderEnvelope"

  /organizations/{orgId}/change-orders/{id}/items:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    put:
      tags: [Change orders]
      operationId: replaceChangeOrderItems
      summary: Atomically replace draft change-order items
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReplaceChangeOrderItemsRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Change-order items replaced and totals recalculated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChangeOrderEnvelope"

  /organizations/{orgId}/change-orders/{id}/actions/send:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Change orders]
      operationId: sendChangeOrder
      summary: Send a change order and create a scoped public token
      security: *employeeWriteSecurity
      responses:
        <<: *standardErrors
        "200":
          description: Change order sent.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChangeOrderEnvelope"

  /organizations/{orgId}/change-orders/{id}/actions/cancel:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    post:
      tags: [Change orders]
      operationId: cancelChangeOrder
      summary: Cancel a change order with a reason
      security: *employeeWriteSecurity
      requestBody:
        $ref: "#/components/requestBodies/ReasonRequired"
      responses:
        <<: *standardErrors
        "200":
          description: Change order cancelled.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChangeOrderEnvelope"

  /public/change-orders/{token}:
    parameters:
      - $ref: "#/components/parameters/PublicToken"
    get:
      tags: [Change orders]
      operationId: getPublicChangeOrder
      summary: Get sanitized customer-visible change-order data
      security: []
      responses:
        <<: *standardErrors
        "200":
          description: Public change order.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicChangeOrderEnvelope"

  /public/change-orders/{token}/responses:
    parameters:
      - $ref: "#/components/parameters/PublicToken"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Change orders]
      operationId: respondToPublicChangeOrder
      summary: Accept or reject a public change order once
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PublicChangeOrderResponseRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Customer signoff decision recorded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicDecisionEnvelope"

  /organizations/{orgId}/payment-milestones:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Payments]
      operationId: listPaymentMilestones
      summary: List project payment milestones
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
        - name: projectId
          in: query
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/PaymentStatus"
        - name: dueFrom
          in: query
          schema:
            $ref: "#/components/schemas/BusinessDate"
        - name: dueTo
          in: query
          schema:
            $ref: "#/components/schemas/BusinessDate"
      responses:
        <<: *standardErrors
        "200":
          description: Payment milestone page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentMilestoneListEnvelope"
    post:
      tags: [Payments]
      operationId: createPaymentMilestone
      summary: Create a project payment milestone
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IdempotencyKeyOptional"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePaymentMilestoneRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Payment milestone created.
          headers:
            Location:
              $ref: "#/components/headers/Location"
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentMilestoneEnvelope"

  /organizations/{orgId}/payment-milestones/{id}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    get:
      tags: [Payments]
      operationId: getPaymentMilestone
      summary: Get a payment milestone
      responses:
        <<: *standardErrors
        "200":
          description: Payment milestone.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentMilestoneEnvelope"
    patch:
      tags: [Payments]
      operationId: updatePaymentMilestone
      summary: Update editable pending or invoiced payment fields
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePaymentMilestoneRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Payment milestone updated.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentMilestoneEnvelope"

  /organizations/{orgId}/payment-milestones/{id}/actions/invoice:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Payments]
      operationId: invoicePaymentMilestone
      summary: Mark a pending milestone as invoiced
      security: *employeeWriteSecurity
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InvoicePaymentMilestoneRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Milestone invoiced.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentMilestoneEnvelope"

  /organizations/{orgId}/payment-milestones/{id}/actions/mark-paid:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Payments]
      operationId: markPaymentMilestonePaid
      summary: Record an out-of-band payment without processing card data
      description: Card numbers, CVV, banking passwords and equivalent secrets are rejected.
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MarkPaymentPaidRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Payment recorded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentMilestoneEnvelope"

  /organizations/{orgId}/payment-milestones/{id}/actions/waive:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    post:
      tags: [Payments]
      operationId: waivePaymentMilestone
      summary: Waive a payment milestone with a reason
      security: *employeeWriteSecurity
      requestBody:
        $ref: "#/components/requestBodies/ReasonRequired"
      responses:
        <<: *standardErrors
        "200":
          description: Milestone waived.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentMilestoneEnvelope"

  /organizations/{orgId}/payment-milestones/{id}/actions/reverse-payment:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Payments]
      operationId: reversePaymentMilestone
      summary: Reverse a recorded payment with an audit reason
      security: *employeeWriteSecurity
      requestBody:
        $ref: "#/components/requestBodies/ReasonRequired"
      responses:
        <<: *standardErrors
        "200":
          description: Recorded payment reversed; no external refund is attempted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentMilestoneEnvelope"

  /organizations/{orgId}/payment-milestones/{id}/actions/cancel:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    post:
      tags: [Payments]
      operationId: cancelPaymentMilestone
      summary: Cancel an unpaid payment milestone
      security: *employeeWriteSecurity
      requestBody:
        $ref: "#/components/requestBodies/ReasonRequired"
      responses:
        <<: *standardErrors
        "200":
          description: Milestone cancelled.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentMilestoneEnvelope"

  /organizations/{orgId}/maintenance-plans:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Maintenance]
      operationId: listMaintenancePlans
      summary: List recurring maintenance plans across supported trades
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/MaintenanceStatus"
        - name: dueBefore
          in: query
          schema:
            $ref: "#/components/schemas/BusinessDate"
        - name: assetId
          in: query
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: customerId
          in: query
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        <<: *standardErrors
        "200":
          description: Maintenance plan page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MaintenancePlanListEnvelope"
    post:
      tags: [Maintenance]
      operationId: createMaintenancePlan
      summary: Create a reusable maintenance or follow-up plan
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IdempotencyKeyOptional"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateMaintenancePlanRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Maintenance plan created.
          headers:
            Location:
              $ref: "#/components/headers/Location"
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MaintenancePlanEnvelope"

  /organizations/{orgId}/maintenance-plans/{id}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    get:
      tags: [Maintenance]
      operationId: getMaintenancePlan
      summary: Get a maintenance plan
      responses:
        <<: *standardErrors
        "200":
          description: Maintenance plan.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MaintenancePlanEnvelope"
    patch:
      tags: [Maintenance]
      operationId: updateMaintenancePlan
      summary: Update an active or paused maintenance plan
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateMaintenancePlanRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Maintenance plan updated.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MaintenancePlanEnvelope"

  /organizations/{orgId}/maintenance-plans/{id}/actions/pause:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    post:
      tags: [Maintenance]
      operationId: pauseMaintenancePlan
      summary: Pause future maintenance reminders
      security: *employeeWriteSecurity
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PauseMaintenancePlanRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Maintenance plan paused.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MaintenancePlanEnvelope"

  /organizations/{orgId}/maintenance-plans/{id}/actions/resume:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    post:
      tags: [Maintenance]
      operationId: resumeMaintenancePlan
      summary: Resume a paused plan with a new next due date
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResumeMaintenancePlanRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Maintenance plan resumed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MaintenancePlanEnvelope"

  /organizations/{orgId}/maintenance-plans/{id}/actions/complete:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Maintenance]
      operationId: completeMaintenancePlanCycle
      summary: Record completed work and calculate or set the next due date
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompleteMaintenancePlanRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Maintenance cycle completed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MaintenancePlanEnvelope"

  /organizations/{orgId}/maintenance-plans/{id}/actions/cancel:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    post:
      tags: [Maintenance]
      operationId: cancelMaintenancePlan
      summary: Permanently cancel a maintenance plan
      security: *employeeWriteSecurity
      requestBody:
        $ref: "#/components/requestBodies/ReasonRequired"
      responses:
        <<: *standardErrors
        "200":
          description: Maintenance plan cancelled.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MaintenancePlanEnvelope"

  /organizations/{orgId}/maintenance-reminders:prepare:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Maintenance]
      operationId: prepareMaintenanceReminders
      summary: Prepare at most 100 consent-checked notification drafts
      security: *employeeWriteSecurity
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PrepareMaintenanceRemindersRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Reminder drafts prepared but not sent.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PreparedNotificationsEnvelope"

  /organizations/{orgId}/notifications:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Notifications]
      operationId: listNotifications
      summary: List redacted transaction and follow-up notifications
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/NotificationStatus"
        - name: channel
          in: query
          schema:
            $ref: "#/components/schemas/NotificationChannel"
        - name: relatedType
          in: query
          schema:
            type: string
            maxLength: 64
        - name: relatedId
          in: query
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: scheduledFrom
          in: query
          schema:
            $ref: "#/components/schemas/Timestamp"
      responses:
        <<: *standardErrors
        "200":
          description: Notification page without provider credentials or full recipient identifiers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationListEnvelope"

  /organizations/{orgId}/notifications/{id}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    get:
      tags: [Notifications]
      operationId: getNotification
      summary: Get a notification with redacted attempts
      responses:
        <<: *standardErrors
        "200":
          description: Redacted notification detail.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationEnvelope"

  /organizations/{orgId}/notifications:approve:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Notifications]
      operationId: approveNotifications
      summary: Approve at most 100 pending consent-checked drafts for dispatch
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApproveNotificationsRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Drafts approved atomically where eligible.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationBatchEnvelope"

  /organizations/{orgId}/notifications/{id}/actions/retry:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [Notifications]
      operationId: retryNotification
      summary: Requeue a failed notification under a row lock
      security: *employeeWriteSecurity
      responses:
        <<: *standardErrors
        "200":
          description: Failed notification requeued.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationEnvelope"

  /organizations/{orgId}/notifications/{id}/actions/cancel:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    post:
      tags: [Notifications]
      operationId: cancelNotification
      summary: Cancel a pending or failed notification
      security: *employeeWriteSecurity
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CancelNotificationRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Notification cancelled.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationEnvelope"

  /organizations/{orgId}/line-channels:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [LINE]
      operationId: listLineChannels
      summary: List LINE channel metadata without secrets
      responses:
        <<: *standardErrors
        "200":
          description: LINE channel metadata.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LineChannelListEnvelope"
    post:
      tags: [LINE]
      operationId: createLineChannel
      summary: Bind encrypted write-only LINE credentials
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateLineChannelRequest"
      responses:
        <<: *standardErrors
        "201":
          description: Channel metadata created; credentials are never returned.
          headers:
            Location:
              $ref: "#/components/headers/Location"
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LineChannelEnvelope"

  /organizations/{orgId}/line-channels/{id}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    get:
      tags: [LINE]
      operationId: getLineChannel
      summary: Get non-sensitive LINE channel metadata
      responses:
        <<: *standardErrors
        "200":
          description: LINE channel metadata.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LineChannelEnvelope"
    patch:
      tags: [LINE]
      operationId: updateLineChannel
      summary: Update non-secret LINE channel metadata
      security: *employeeWriteSecurity
      parameters:
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLineChannelRequest"
      responses:
        <<: *standardErrors
        "200":
          description: LINE channel metadata updated.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LineChannelEnvelope"

  /organizations/{orgId}/line-channels/{id}/actions/verify:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
    post:
      tags: [LINE]
      operationId: verifyLineChannel
      summary: Verify stored credentials with LINE without exposing them
      security: *employeeWriteSecurity
      responses:
        <<: *standardErrors
        "200":
          description: Verification result and safe channel metadata.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LineChannelEnvelope"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"

  /organizations/{orgId}/line-channels/{id}/actions/rotate-token:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
      - $ref: "#/components/parameters/IdempotencyKey"
    post:
      tags: [LINE]
      operationId: rotateLineChannelToken
      summary: Replace the encrypted access token with a write-only value
      security: *employeeWriteSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RotateLineTokenRequest"
      responses:
        <<: *standardErrors
        "200":
          description: Token replaced and credential version incremented.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LineChannelEnvelope"

  /organizations/{orgId}/line-channels/{id}/actions/disable:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - $ref: "#/components/parameters/ResourceId"
      - $ref: "#/components/parameters/IfMatch"
    post:
      tags: [LINE]
      operationId: disableLineChannel
      summary: Disable webhook processing and outbound use for a channel
      security: *employeeWriteSecurity
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DisableLineChannelRequest"
      responses:
        <<: *standardErrors
        "200":
          description: LINE channel disabled.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LineChannelEnvelope"

  /webhooks/line/{lineChannelId}:
    parameters:
      - $ref: "#/components/parameters/LineChannelId"
    post:
      tags: [LINE]
      operationId: receiveLineWebhook
      summary: Authenticate and durably accept a LINE webhook batch
      description: |
        The implementation verifies HMAC-SHA256 over the raw bytes before JSON parsing,
        limits the body to 1 MiB and 100 events, and deduplicates provider event ids.
        Both a new delivery and a valid replay receive the same acknowledgement.
      security: *lineWebhookSecurity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LineWebhookPayload"
      responses:
        "200":
          description: Valid delivery accepted or previously accepted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookAcknowledgement"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "429":
          $ref: "#/components/responses/RateLimited"
        "500":
          $ref: "#/components/responses/InternalError"

  /organizations/{orgId}/dashboard:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Dashboard]
      operationId: getDashboard
      summary: Get the four fixed business KPIs and actionable queues
      parameters:
        - $ref: "#/components/parameters/ReportFrom"
        - $ref: "#/components/parameters/ReportTo"
      responses:
        <<: *standardErrors
        "200":
          description: Dashboard values with numerator, denominator, window and timezone.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardEnvelope"

  /organizations/{orgId}/reports/funnel:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Dashboard]
      operationId: getFunnelReport
      summary: Get request-to-quote-to-accept-to-completion counts
      parameters:
        - $ref: "#/components/parameters/ReportFrom"
        - $ref: "#/components/parameters/ReportTo"
      responses:
        <<: *standardErrors
        "200":
          description: Funnel report.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FunnelReportEnvelope"

  /organizations/{orgId}/reports/operations:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Dashboard]
      operationId: getOperationsReport
      summary: Get response-time, completion and aggregate technician-load metrics
      parameters:
        - $ref: "#/components/parameters/ReportFrom"
        - $ref: "#/components/parameters/ReportTo"
      responses:
        <<: *standardErrors
        "200":
          description: Operations report without default sensitive employee ranking.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperationsReportEnvelope"

  /organizations/{orgId}/reports/retention:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Dashboard]
      operationId: getRetentionReport
      summary: Get maintenance due and reminder-to-booking conversion metrics
      parameters:
        - $ref: "#/components/parameters/ReportFrom"
        - $ref: "#/components/parameters/ReportTo"
      responses:
        <<: *standardErrors
        "200":
          description: Retention report.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RetentionReportEnvelope"

  /internal/workers/notifications:dispatch:
    post:
      tags: [Notifications]
      operationId: dispatchNotificationWorkerBatch
      summary: Claim and dispatch a bounded notification batch
      security:
        - workerBearer: []
      requestBody:
        $ref: "#/components/requestBodies/WorkerBatch"
      responses:
        <<: *standardErrors
        "200":
          $ref: "#/components/responses/WorkerBatchResult"

  /internal/workers/line-webhooks:process:
    post:
      tags: [LINE]
      operationId: processLineWebhookWorkerBatch
      summary: Claim and process durable LINE webhook inbox events
      security:
        - workerBearer: []
      requestBody:
        $ref: "#/components/requestBodies/WorkerBatch"
      responses:
        <<: *standardErrors
        "200":
          $ref: "#/components/responses/WorkerBatchResult"

  /internal/workers/maintenance:scan:
    post:
      tags: [Maintenance]
      operationId: scanMaintenanceWorkerBatch
      summary: Scan organization-local due dates and prepare reminder drafts
      security:
        - workerBearer: []
      requestBody:
        $ref: "#/components/requestBodies/WorkerBatch"
      responses:
        <<: *standardErrors
        "200":
          $ref: "#/components/responses/WorkerBatchResult"

  /internal/workers/payments:mark-overdue:
    post:
      tags: [Payments]
      operationId: markOverduePaymentsWorkerBatch
      summary: Idempotently mark eligible payment milestones overdue
      security:
        - workerBearer: []
      requestBody:
        $ref: "#/components/requestBodies/WorkerBatch"
      responses:
        <<: *standardErrors
        "200":
          $ref: "#/components/responses/WorkerBatchResult"

  /internal/workers/media:cleanup:
    post:
      tags: [Photos]
      operationId: cleanupMediaWorkerBatch
      summary: Clean up expired pending uploads and deleted media
      security:
        - workerBearer: []
      requestBody:
        $ref: "#/components/requestBodies/WorkerBatch"
      responses:
        <<: *standardErrors
        "200":
          $ref: "#/components/responses/WorkerBatchResult"

  /internal/workers/tokens:cleanup:
    post:
      tags: [Session]
      operationId: cleanupTokensWorkerBatch
      summary: Remove expired public tokens and idempotency records
      security:
        - workerBearer: []
      requestBody:
        $ref: "#/components/requestBodies/WorkerBatch"
      responses:
        <<: *standardErrors
        "200":
          $ref: "#/components/responses/WorkerBatchResult"

components:
  securitySchemes:
    employeeSession:
      type: apiKey
      in: cookie
      name: session
      description: |
        Logical HttpOnly, Secure, SameSite session cookie issued by the server-side
        Supabase Auth adapter. Provider access and refresh tokens are never exposed.
    csrfToken:
      type: apiKey
      in: header
      name: X-CSRF-Token
      description: Required for employee mutations in addition to the session cookie.
    lineSignature:
      type: apiKey
      in: header
      name: X-Line-Signature
      description: Base64 HMAC-SHA256 signature verified over the exact raw request bytes.
    workerBearer:
      type: http
      scheme: bearer
      bearerFormat: short-lived service JWT
      description: Audience-restricted workload identity; never accepted by employee routes.
    publicCapabilityBearer:
      type: http
      scheme: bearer
      bearerFormat: 256-bit opaque capability
      description: Raw public quote capability carried only in Authorization; never in the HTTP path or logs.

  parameters:
    OrgId:
      name: orgId
      in: path
      required: true
      description: Organization tenant boundary checked against the active membership.
      schema:
        $ref: "#/components/schemas/Uuid"
    ResourceId:
      name: id
      in: path
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    CustomerId:
      name: customerId
      in: path
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    LocationId:
      name: locationId
      in: path
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    AssetId:
      name: assetId
      in: path
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    AssignmentId:
      name: assignmentId
      in: path
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    VersionId:
      name: versionId
      in: path
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    ItemId:
      name: itemId
      in: path
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    PhotoId:
      name: photoId
      in: path
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    PublicToken:
      name: token
      in: path
      required: true
      description: Opaque, scoped, expiring and revocable public capability token.
      schema:
        type: string
        minLength: 32
        maxLength: 512
        pattern: "^[A-Za-z0-9._~-]+$"
    LineChannelId:
      name: lineChannelId
      in: path
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    IfMatch:
      name: If-Match
      in: header
      required: true
      description: Strong ETag containing the last observed positive lock version.
      schema:
        type: string
        pattern: '^"[1-9][0-9]*"$'
        examples: ['"3"']
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      description: Scoped to actor, organization, method and route template.
      schema:
        type: string
        minLength: 8
        maxLength: 128
        pattern: "^[A-Za-z0-9._:-]+$"
    IdempotencyKeyOptional:
      name: Idempotency-Key
      in: header
      required: false
      description: Recommended on create operations; identical retries replay the first result.
      schema:
        type: string
        minLength: 8
        maxLength: 128
        pattern: "^[A-Za-z0-9._:-]+$"
    Cursor:
      name: cursor
      in: query
      description: Opaque signed keyset cursor bound to tenant, route, sort and filters.
      schema:
        type: string
        minLength: 16
        maxLength: 1024
        pattern: "^[A-Za-z0-9_-]+$"
    Limit:
      name: limit
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20
    SearchQuery:
      name: q
      in: query
      description: Trimmed query over the endpoint-specific allowlist of fields.
      schema:
        type: string
        minLength: 2
        maxLength: 100
    ReportFrom:
      name: from
      in: query
      required: true
      schema:
        $ref: "#/components/schemas/BusinessDate"
    ReportTo:
      name: to
      in: query
      required: true
      description: Inclusive business date; the from/to window cannot exceed 366 days.
      schema:
        $ref: "#/components/schemas/BusinessDate"

  headers:
    ETag:
      description: Strong aggregate lock-version ETag used by If-Match.
      required: true
      schema:
        type: string
        pattern: '^"[1-9][0-9]*"$'
    Location:
      description: Relative URL of the created resource.
      required: true
      schema:
        type: string
        format: uri-reference
    RequestId:
      description: Non-secret request correlation identifier.
      required: true
      schema:
        type: string
        minLength: 8
        maxLength: 128
    RetryAfter:
      description: Number of seconds before a retry is allowed.
      required: true
      schema:
        type: integer
        minimum: 1

  requestBodies:
    ReasonRequired:
      required: true
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ReasonRequest"
    WorkerBatch:
      required: true
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/WorkerBatchRequest"

  responses:
    NoContent:
      description: Operation completed with no response body.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
    WorkerBatchResult:
      description: Worker batch counters; no payload or recipient data is returned.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/WorkerResultEnvelope"
    BadRequest:
      description: Malformed request, invalid cursor or unsupported filter combination.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
    Unauthorized:
      description: Authentication or webhook signature is absent or invalid.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
    Forbidden:
      description: Authenticated actor lacks the required organization role or CSRF proof.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
    NotFound:
      description: Resource does not exist in the authorized tenant or is intentionally hidden.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
    Conflict:
      description: State transition, schedule or active-version conflict.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
    PreconditionFailed:
      description: If-Match lock version is stale.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
    PayloadTooLarge:
      description: Request or upload exceeds the endpoint limit.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
    UnsupportedMediaType:
      description: Content type or upload media type is not accepted.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
    UnprocessableEntity:
      description: Schema validation or business precondition failed.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
    PreconditionRequired:
      description: A required If-Match header is missing.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
    RateLimited:
      description: Rate or abuse limit exceeded.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
        Retry-After:
          $ref: "#/components/headers/RetryAfter"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
    InternalError:
      description: Unexpected server error without implementation details.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
    ProviderUnavailable:
      description: A synchronous external-provider verification is temporarily unavailable.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"

  schemas:
    Uuid:
      type: string
      format: uuid
    Timestamp:
      type: string
      format: date-time
    BusinessDate:
      type: string
      format: date
    MoneyMinor:
      type: string
      description: Non-negative integer minor units; never a JSON floating-point number.
      pattern: "^(0|[1-9][0-9]*)$"
      maxLength: 19
      examples: ["2490"]
    SignedMoneyMinor:
      type: string
      description: Signed integer minor units used for additive or deductive change orders.
      pattern: "^-?(0|[1-9][0-9]*)$"
      maxLength: 20
    Quantity:
      type: string
      description: Positive decimal quantity with at most three fractional digits.
      pattern: "^(0|[1-9][0-9]*)(\\.[0-9]{1,3})?$"
      maxLength: 24
      examples: ["1.000"]
    TaxRate:
      type: string
      description: Decimal fraction from zero through one, with at most four fractional digits.
      pattern: "^(0(\\.[0-9]{1,4})?|1(\\.0{1,4})?)$"
      examples: ["0.0500"]
    Currency:
      type: string
      pattern: "^[A-Z]{3}$"
      default: TWD
      examples: [TWD]
    E164Phone:
      type: string
      pattern: "^\\+[1-9][0-9]{7,14}$"
      examples: ["+886912345678"]
    LockVersion:
      type: integer
      minimum: 1
    NonEmptyText:
      type: string
      minLength: 1
      maxLength: 5000
    ShortText:
      type: string
      minLength: 1
      maxLength: 200

    Role:
      type: string
      enum: [owner, admin, dispatcher, technician, accountant, viewer]
    MembershipStatus:
      type: string
      enum: [invited, active, suspended, removed]
    IndustryTemplate:
      type: string
      description: A starting configuration, not a hard-coded business vertical.
      enum: [general_field_service, cooling, plumbing, waterproofing, renovation]
    OrganizationStatus:
      type: string
      enum: [active, suspended, closed]
    CustomerKind:
      type: string
      enum: [individual, company]
    CustomerSource:
      type: string
      enum: [line, phone, web, referral, manual, import]
    AssetStatus:
      type: string
      enum: [active, inactive, retired]
    Priority:
      type: string
      enum: [low, normal, high, urgent]
    ServiceRequestSource:
      type: string
      enum: [line, phone, web, referral, manual]
    ServiceRequestStatus:
      type: string
      enum: [new, triaged, quoting, quoted, converted, declined, cancelled]
    ProjectStatus:
      type: string
      enum: [draft, active, on_hold, completed, cancelled]
    WorkOrderStatus:
      type: string
      enum: [draft, scheduled, dispatched, en_route, on_site, paused, completed, cancelled]
    WorkOrderAction:
      type: string
      enum: [dispatch, enRoute, arrive, pause, resume, complete, cancel, reopen]
    AssignmentDuty:
      type: string
      enum: [lead, technician, helper, observer]
    AssignmentStatus:
      type: string
      enum: [assigned, accepted, declined, checked_in, completed, cancelled]
    ChecklistItemType:
      type: string
      enum: [boolean, text, number, single_choice, multi_choice, photo]
    ChecklistStatus:
      type: string
      enum: [pending, in_progress, completed, reopened]
    PhotoCategory:
      type: string
      enum: [intake, before, after, issue, receipt, signature, other]
    PhotoStatus:
      type: string
      enum: [pending, processing, ready, quarantined, failed, deleted]
    QuoteStatus:
      type: string
      enum: [draft, sent, viewed, accepted, rejected, expired, cancelled]
    QuoteVersionStatus:
      type: string
      enum: [draft, sent, superseded, accepted, rejected, expired, cancelled]
    QuoteApprovalStatus:
      type: string
      enum: [not_submitted, pending, approved, changes_requested]
    PublicDecision:
      type: string
      enum: [accept, reject]
    ChangeOrderKind:
      type: string
      enum: [addition, deduction]
    ChangeOrderStatus:
      type: string
      enum: [draft, sent, accepted, rejected, cancelled]
    PaymentStatus:
      type: string
      enum: [pending, invoiced, overdue, paid, waived, cancelled]
    PaymentMethod:
      type: string
      enum: [cash, transfer, card, other]
    MaintenanceStatus:
      type: string
      enum: [active, paused, completed, cancelled]
    NotificationStatus:
      type: string
      enum: [pending, processing, sent, delivered, failed, cancelled]
    NotificationChannel:
      type: string
      enum: [line, in_app, email, sms]
    NotificationApprovalStatus:
      type: string
      enum: [not_required, pending, approved, rejected]
    LineChannelStatus:
      type: string
      enum: [pending, active, disabled, error]

    ListMeta:
      type: object
      additionalProperties: false
      properties:
        nextCursor:
          type: [string, "null"]
          minLength: 16
          maxLength: 1024
        hasMore:
          type: boolean
      required: [nextCursor, hasMore]
    ProblemFieldError:
      type: object
      additionalProperties: false
      properties:
        path:
          type: string
          maxLength: 500
        code:
          type: string
          pattern: "^[A-Z][A-Z0-9_]*$"
        message:
          type: string
          maxLength: 1000
      required: [path, code, message]
    ProblemDetails:
      type: object
      additionalProperties: false
      description: RFC 9457-compatible error with stable application code and no stack traces.
      properties:
        type:
          type: string
          format: uri-reference
        title:
          type: string
          maxLength: 200
        status:
          type: integer
          minimum: 400
          maximum: 599
        detail:
          type: string
          maxLength: 2000
        instance:
          type: string
          format: uri-reference
        code:
          type: string
          pattern: "^[A-Z][A-Z0-9_]*$"
        requestId:
          type: string
          minLength: 8
          maxLength: 128
        errors:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/ProblemFieldError"
        latestLockVersion:
          $ref: "#/components/schemas/LockVersion"
      required: [type, title, status, code, requestId]
    ReasonRequest:
      type: object
      additionalProperties: false
      properties:
        reason:
          type: string
          minLength: 2
          maxLength: 1000
      required: [reason]
    ActorSummary:
      type: object
      additionalProperties: false
      properties:
        type:
          type: string
          enum: [user, customer, system, line]
        id:
          type: [string, "null"]
          format: uuid
        displayName:
          type: [string, "null"]
          maxLength: 200
      required: [type, id, displayName]
    Event:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        aggregateType:
          type: string
          maxLength: 64
        aggregateId:
          $ref: "#/components/schemas/Uuid"
        eventType:
          type: string
          maxLength: 160
        occurredAt:
          $ref: "#/components/schemas/Timestamp"
        recordedAt:
          $ref: "#/components/schemas/Timestamp"
        chainSequence:
          type: integer
          minimum: 1
        actor:
          $ref: "#/components/schemas/ActorSummary"
        summary:
          type: string
          maxLength: 1000
      required: [id, aggregateType, aggregateId, eventType, occurredAt, recordedAt, chainSequence, actor, summary]
    EventListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Event"
        meta:
          $ref: "#/components/schemas/ListMeta"
      required: [data, meta]
    PilotServiceRequestEvent:
      type: object
      additionalProperties: false
      description: Redacted append-only M3 request timeline row.
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        eventType:
          type: string
          minLength: 1
          maxLength: 160
        actorType:
          type: string
          enum: [user, customer, system, line]
        actorUserId:
          type: [string, "null"]
          format: uuid
        occurredAt:
          $ref: "#/components/schemas/Timestamp"
        chainSequence:
          type: integer
          minimum: 1
        payload:
          type: object
          description: Allowlisted event payload with the raw submission copy removed.
          additionalProperties: true
      required: [id, eventType, actorType, actorUserId, occurredAt, chainSequence, payload]
    PilotServiceRequestEventListMeta:
      type: object
      additionalProperties: false
      properties:
        hasMore:
          type: boolean
        nextCursor:
          type: [integer, "null"]
          minimum: 1
      required: [hasMore, nextCursor]
    PilotServiceRequestEventListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/PilotServiceRequestEvent"
        meta:
          $ref: "#/components/schemas/PilotServiceRequestEventListMeta"
      required: [data, meta]

    Health:
      type: object
      additionalProperties: false
      properties:
        status:
          type: string
          enum: [ok]
        version:
          type: string
          maxLength: 100
        time:
          $ref: "#/components/schemas/Timestamp"
      required: [status, version, time]
    HealthEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/Health"
      required: [data]

    SessionUser:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        email:
          type: string
          format: email
          maxLength: 320
        displayName:
          type: string
          minLength: 1
          maxLength: 200
      required: [id, email, displayName]
    Membership:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        organizationId:
          $ref: "#/components/schemas/Uuid"
        userId:
          type: [string, "null"]
          format: uuid
        email:
          type: string
          format: email
          maxLength: 320
        displayName:
          type: string
          maxLength: 200
        role:
          $ref: "#/components/schemas/Role"
        status:
          $ref: "#/components/schemas/MembershipStatus"
        phone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
        lockVersion:
          $ref: "#/components/schemas/LockVersion"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, organizationId, userId, email, displayName, role, status, phone, lockVersion, createdAt, updatedAt]
    PilotAssignableMember:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        displayName:
          type: string
          minLength: 1
          maxLength: 80
        role:
          type: string
          enum: [owner, admin, dispatcher, technician]
        status:
          type: string
          const: active
      required: [id, displayName, role, status]
    PilotAssignableMemberListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/PilotAssignableMember"
      required: [data]
    Organization:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        name:
          type: string
          minLength: 1
          maxLength: 200
        legalName:
          type: [string, "null"]
          maxLength: 200
        phone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
        timezone:
          type: string
          minLength: 1
          maxLength: 64
          examples: [Asia/Taipei]
        locale:
          type: string
          pattern: "^[a-z]{2}(?:-[A-Z]{2})?$"
          examples: [zh-TW]
        currency:
          $ref: "#/components/schemas/Currency"
        industryTemplate:
          $ref: "#/components/schemas/IndustryTemplate"
        status:
          $ref: "#/components/schemas/OrganizationStatus"
        lockVersion:
          $ref: "#/components/schemas/LockVersion"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, name, legalName, phone, timezone, locale, currency, industryTemplate, status, lockVersion, createdAt, updatedAt]
    Session:
      type: object
      additionalProperties: false
      properties:
        user:
          $ref: "#/components/schemas/SessionUser"
        memberships:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/Membership"
      required: [user, memberships]
    SessionEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/Session"
      required: [data]
    OrganizationEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/Organization"
      required: [data]
    OrganizationCreated:
      type: object
      additionalProperties: false
      properties:
        organization:
          $ref: "#/components/schemas/Organization"
        membership:
          $ref: "#/components/schemas/Membership"
      required: [organization, membership]
    OrganizationCreatedEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/OrganizationCreated"
      required: [data]
    PilotOrganizationSettings:
      type: object
      additionalProperties: false
      properties:
        organizationId:
          $ref: "#/components/schemas/Uuid"
        name:
          type: string
          minLength: 1
          maxLength: 120
        industryTemplate:
          $ref: "#/components/schemas/IndustryTemplate"
        intakeHeadline:
          type: string
          minLength: 1
          maxLength: 200
        privacyNotice:
          type: string
          minLength: 1
          maxLength: 5000
        lockVersion:
          type: integer
          minimum: 1
      required:
        - organizationId
        - name
        - industryTemplate
        - intakeHeadline
        - privacyNotice
        - lockVersion
    PilotOrganizationSettingsEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/PilotOrganizationSettings"
      required: [data]
    UpdatePilotOrganizationSettingsRequest:
      type: object
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 120
        intakeHeadline:
          type: string
          minLength: 1
          maxLength: 200
        privacyNotice:
          type: string
          minLength: 1
          maxLength: 5000
        lockVersion:
          type: integer
          minimum: 1
      required: [name, intakeHeadline, privacyNotice, lockVersion]
    PilotPublicIntakeLink:
      type: object
      additionalProperties: false
      properties:
        publicIntakeUrl:
          type: string
          format: uri
      required: [publicIntakeUrl]
    PilotPublicIntakeLinkEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/PilotPublicIntakeLink"
      required: [data]
    MembershipEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/Membership"
      required: [data]
    MembershipListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Membership"
        meta:
          $ref: "#/components/schemas/ListMeta"
      required: [data, meta]
    CreateOrganizationRequest:
      type: object
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        legalName:
          type: [string, "null"]
          maxLength: 200
        phone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
        timezone:
          type: string
          maxLength: 64
          default: Asia/Taipei
        locale:
          type: string
          default: zh-TW
          pattern: "^[a-z]{2}(?:-[A-Z]{2})?$"
        currency:
          $ref: "#/components/schemas/Currency"
        industryTemplate:
          $ref: "#/components/schemas/IndustryTemplate"
      required: [name, timezone, locale, currency, industryTemplate]
    UpdateOrganizationRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        legalName:
          type: [string, "null"]
          maxLength: 200
        phone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
        timezone:
          type: string
          minLength: 1
          maxLength: 64
        locale:
          type: string
          pattern: "^[a-z]{2}(?:-[A-Z]{2})?$"
    InviteMembershipRequest:
      type: object
      additionalProperties: false
      properties:
        email:
          type: string
          format: email
          maxLength: 320
        displayName:
          type: string
          minLength: 1
          maxLength: 200
        role:
          $ref: "#/components/schemas/Role"
        phone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
      required: [email, displayName, role]
    UpdateMembershipRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        displayName:
          type: string
          minLength: 1
          maxLength: 200
        phone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
    ChangeRoleRequest:
      type: object
      additionalProperties: false
      properties:
        role:
          $ref: "#/components/schemas/Role"
      required: [role]

    Customer:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        kind:
          $ref: "#/components/schemas/CustomerKind"
        name:
          type: string
          minLength: 1
          maxLength: 200
        companyName:
          type: [string, "null"]
          maxLength: 200
        phone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
        email:
          type: [string, "null"]
          format: email
          maxLength: 320
        source:
          $ref: "#/components/schemas/CustomerSource"
        notes:
          type: [string, "null"]
          maxLength: 5000
        marketingConsent:
          type: boolean
        marketingConsentAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        lockVersion:
          $ref: "#/components/schemas/LockVersion"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, kind, name, companyName, phone, email, source, notes, marketingConsent, marketingConsentAt, lockVersion, createdAt, updatedAt]
    CreateCustomerRequest:
      type: object
      additionalProperties: false
      properties:
        kind:
          $ref: "#/components/schemas/CustomerKind"
        name:
          type: string
          minLength: 1
          maxLength: 200
        companyName:
          type: [string, "null"]
          maxLength: 200
        phone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
        email:
          type: [string, "null"]
          format: email
          maxLength: 320
        source:
          $ref: "#/components/schemas/CustomerSource"
        notes:
          type: [string, "null"]
          maxLength: 5000
        marketingConsent:
          type: boolean
          default: false
      required: [kind, name, source]
    UpdateCustomerRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        kind:
          $ref: "#/components/schemas/CustomerKind"
        name:
          type: string
          minLength: 1
          maxLength: 200
        companyName:
          type: [string, "null"]
          maxLength: 200
        phone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
        email:
          type: [string, "null"]
          format: email
          maxLength: 320
        source:
          $ref: "#/components/schemas/CustomerSource"
        notes:
          type: [string, "null"]
          maxLength: 5000
        marketingConsent:
          type: boolean
    CustomerEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/Customer"
      required: [data]
    CustomerListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Customer"
        meta:
          $ref: "#/components/schemas/ListMeta"
      required: [data, meta]
    PilotCustomer:
      type: object
      additionalProperties: false
      description: Exact customer projection implemented by the M3 triage workspace.
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        customerNo:
          type: string
          minLength: 1
          maxLength: 40
        kind:
          $ref: "#/components/schemas/CustomerKind"
        name:
          type: string
          minLength: 1
          maxLength: 120
        phone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
        email:
          type: [string, "null"]
          format: email
          maxLength: 254
        companyName:
          type: [string, "null"]
          maxLength: 200
        source:
          $ref: "#/components/schemas/CustomerSource"
        notes:
          type: string
          maxLength: 5000
        lastContactAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        lockVersion:
          $ref: "#/components/schemas/LockVersion"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, customerNo, kind, name, phone, email, companyName, source, notes, lastContactAt, lockVersion, createdAt, updatedAt]
    PilotCreateCustomerRequest:
      type: object
      additionalProperties: false
      description: Minimal real customer creation input used while triaging an unbound request.
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 120
        phone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
      required: [name]
    PilotCustomerEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/PilotCustomer"
      required: [data]
    PilotCustomerListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          maxItems: 50
          items:
            $ref: "#/components/schemas/PilotCustomer"
      required: [data]
    PilotCustomerLocation:
      type: object
      additionalProperties: false
      description: Exact location projection used by the M3 customer confirmation step.
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        customerId:
          $ref: "#/components/schemas/Uuid"
        label:
          type: string
          minLength: 1
          maxLength: 80
        contactName:
          type: [string, "null"]
          maxLength: 120
        contactPhone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
        postalCode:
          type: [string, "null"]
          maxLength: 12
        county:
          type: [string, "null"]
          maxLength: 80
        district:
          type: [string, "null"]
          maxLength: 80
        addressLine:
          type: string
          minLength: 1
          maxLength: 300
        accessNotes:
          type: string
          maxLength: 2000
        isDefault:
          type: boolean
        lockVersion:
          $ref: "#/components/schemas/LockVersion"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, customerId, label, contactName, contactPhone, postalCode, county, district, addressLine, accessNotes, isDefault, lockVersion, createdAt, updatedAt]
    PilotCustomerLocationListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/PilotCustomerLocation"
      required: [data]
    PilotCustomerAsset:
      type: object
      additionalProperties: false
      description: Exact asset projection used by the M3 customer confirmation step.
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        customerId:
          $ref: "#/components/schemas/Uuid"
        locationId:
          $ref: "#/components/schemas/Uuid"
        assetNo:
          type: string
          minLength: 1
          maxLength: 40
        assetType:
          type: string
          enum: [air_conditioner, water_heater, pump, appliance, other]
        name:
          type: string
          minLength: 1
          maxLength: 160
        brand:
          type: [string, "null"]
          maxLength: 120
        model:
          type: [string, "null"]
          maxLength: 120
        serialNumber:
          type: [string, "null"]
          maxLength: 120
        installedOn:
          anyOf:
            - $ref: "#/components/schemas/BusinessDate"
            - type: "null"
        status:
          $ref: "#/components/schemas/AssetStatus"
        lockVersion:
          $ref: "#/components/schemas/LockVersion"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, customerId, locationId, assetNo, assetType, name, brand, model, serialNumber, installedOn, status, lockVersion, createdAt, updatedAt]
    PilotCustomerAssetListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/PilotCustomerAsset"
      required: [data]
    SimilarCustomer:
      type: object
      additionalProperties: false
      description: >-
        Tenant-scoped hint used during triage to surface possible duplicate
        customers. Hint-only: never auto-merges. matchReason explains why the
        row matched (phone or name).
      properties:
        customerId:
          $ref: "#/components/schemas/Uuid"
        customerNo:
          type: string
          maxLength: 40
        name:
          type: string
          minLength: 1
          maxLength: 200
        phone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
        matchReason:
          type: string
          enum: [phone, name]
      required: [customerId, customerNo, name, phone, matchReason]
    SimilarCustomerListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          maxItems: 20
          items:
            $ref: "#/components/schemas/SimilarCustomer"
      required: [data]

    Location:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        customerId:
          $ref: "#/components/schemas/Uuid"
        label:
          type: string
          minLength: 1
          maxLength: 100
        recipientName:
          type: [string, "null"]
          maxLength: 200
        recipientPhone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
        postalCode:
          type: [string, "null"]
          maxLength: 20
        addressLine1:
          type: string
          minLength: 1
          maxLength: 500
        addressLine2:
          type: [string, "null"]
          maxLength: 500
        accessNotes:
          type: [string, "null"]
          maxLength: 2000
        latitude:
          type: [number, "null"]
          minimum: -90
          maximum: 90
        longitude:
          type: [number, "null"]
          minimum: -180
          maximum: 180
        lockVersion:
          $ref: "#/components/schemas/LockVersion"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, customerId, label, recipientName, recipientPhone, postalCode, addressLine1, addressLine2, accessNotes, latitude, longitude, lockVersion, createdAt, updatedAt]
    CreateLocationRequest:
      type: object
      additionalProperties: false
      properties:
        label:
          type: string
          minLength: 1
          maxLength: 100
        recipientName:
          type: [string, "null"]
          maxLength: 200
        recipientPhone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
        postalCode:
          type: [string, "null"]
          maxLength: 20
        addressLine1:
          type: string
          minLength: 1
          maxLength: 500
        addressLine2:
          type: [string, "null"]
          maxLength: 500
        accessNotes:
          type: [string, "null"]
          maxLength: 2000
        latitude:
          type: [number, "null"]
          minimum: -90
          maximum: 90
        longitude:
          type: [number, "null"]
          minimum: -180
          maximum: 180
      required: [label, addressLine1]
    UpdateLocationRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        label:
          type: string
          minLength: 1
          maxLength: 100
        recipientName:
          type: [string, "null"]
          maxLength: 200
        recipientPhone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
        postalCode:
          type: [string, "null"]
          maxLength: 20
        addressLine1:
          type: string
          minLength: 1
          maxLength: 500
        addressLine2:
          type: [string, "null"]
          maxLength: 500
        accessNotes:
          type: [string, "null"]
          maxLength: 2000
        latitude:
          type: [number, "null"]
          minimum: -90
          maximum: 90
        longitude:
          type: [number, "null"]
          minimum: -180
          maximum: 180
    LocationEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/Location"
      required: [data]
    LocationListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Location"
        meta:
          $ref: "#/components/schemas/ListMeta"
      required: [data, meta]

    Asset:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        customerId:
          $ref: "#/components/schemas/Uuid"
        locationId:
          $ref: "#/components/schemas/Uuid"
        category:
          type: string
          minLength: 1
          maxLength: 100
        brand:
          type: [string, "null"]
          maxLength: 100
        model:
          type: [string, "null"]
          maxLength: 200
        serialNumber:
          type: [string, "null"]
          maxLength: 200
        installedOn:
          anyOf:
            - $ref: "#/components/schemas/BusinessDate"
            - type: "null"
        status:
          $ref: "#/components/schemas/AssetStatus"
        notes:
          type: [string, "null"]
          maxLength: 5000
        lockVersion:
          $ref: "#/components/schemas/LockVersion"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, customerId, locationId, category, brand, model, serialNumber, installedOn, status, notes, lockVersion, createdAt, updatedAt]
    CreateAssetRequest:
      type: object
      additionalProperties: false
      properties:
        customerId:
          $ref: "#/components/schemas/Uuid"
        category:
          type: string
          minLength: 1
          maxLength: 100
        brand:
          type: [string, "null"]
          maxLength: 100
        model:
          type: [string, "null"]
          maxLength: 200
        serialNumber:
          type: [string, "null"]
          maxLength: 200
        installedOn:
          anyOf:
            - $ref: "#/components/schemas/BusinessDate"
            - type: "null"
        status:
          $ref: "#/components/schemas/AssetStatus"
        notes:
          type: [string, "null"]
          maxLength: 5000
      required: [customerId, category, status]
    UpdateAssetRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        category:
          type: string
          minLength: 1
          maxLength: 100
        brand:
          type: [string, "null"]
          maxLength: 100
        model:
          type: [string, "null"]
          maxLength: 200
        serialNumber:
          type: [string, "null"]
          maxLength: 200
        installedOn:
          anyOf:
            - $ref: "#/components/schemas/BusinessDate"
            - type: "null"
        status:
          $ref: "#/components/schemas/AssetStatus"
        notes:
          type: [string, "null"]
          maxLength: 5000
    AssetEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/Asset"
      required: [data]
    AssetListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Asset"
        meta:
          $ref: "#/components/schemas/ListMeta"
      required: [data, meta]

    PreferredTimeWindow:
      type: object
      additionalProperties: false
      properties:
        startsAt:
          $ref: "#/components/schemas/Timestamp"
        endsAt:
          $ref: "#/components/schemas/Timestamp"
        preferenceRank:
          type: integer
          minimum: 1
          maximum: 5
      required: [startsAt, endsAt, preferenceRank]
    ServiceRequest:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        requestNo:
          type: string
          maxLength: 40
        customerId:
          type: [string, "null"]
          format: uuid
        locationId:
          type: [string, "null"]
          format: uuid
        assetId:
          type: [string, "null"]
          format: uuid
        source:
          $ref: "#/components/schemas/ServiceRequestSource"
        status:
          $ref: "#/components/schemas/ServiceRequestStatus"
        priority:
          $ref: "#/components/schemas/Priority"
        category:
          type: string
          minLength: 1
          maxLength: 100
        subject:
          description: Canonical short summary of the request. Maps to the service_requests.subject column.
          type: string
          minLength: 1
          maxLength: 160
        title:
          description: Deprecated alias of subject, retained for backwards compatibility. Always mirrors subject.
          deprecated: true
          type: string
          minLength: 1
          maxLength: 160
        description:
          type: string
          minLength: 1
          maxLength: 5000
        contactName:
          type: string
          minLength: 1
          maxLength: 200
        contactPhone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
        assignedMemberId:
          type: [string, "null"]
          format: uuid
        preferredWindows:
          type: array
          maxItems: 5
          items:
            $ref: "#/components/schemas/PreferredTimeWindow"
        triagedAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        convertedProjectId:
          type: [string, "null"]
          format: uuid
        convertedWorkOrderId:
          type: [string, "null"]
          format: uuid
        lockVersion:
          $ref: "#/components/schemas/LockVersion"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, requestNo, customerId, locationId, assetId, source, status, priority, category, subject, title, description, contactName, contactPhone, assignedMemberId, preferredWindows, triagedAt, convertedProjectId, convertedWorkOrderId, lockVersion, createdAt, updatedAt]
    CreateServiceRequestRequest:
      type: object
      additionalProperties: false
      properties:
        customerId:
          type: [string, "null"]
          format: uuid
        locationId:
          type: [string, "null"]
          format: uuid
        assetId:
          type: [string, "null"]
          format: uuid
        source:
          $ref: "#/components/schemas/ServiceRequestSource"
        priority:
          $ref: "#/components/schemas/Priority"
        category:
          type: string
          minLength: 1
          maxLength: 100
        subject:
          description: Canonical short summary of the request.
          type: string
          minLength: 1
          maxLength: 200
        title:
          description: Deprecated alias of subject, retained for backwards compatibility.
          deprecated: true
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: string
          minLength: 1
          maxLength: 5000
        contactName:
          type: string
          minLength: 1
          maxLength: 200
        contactPhone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
        preferredWindows:
          type: array
          maxItems: 5
          items:
            $ref: "#/components/schemas/PreferredTimeWindow"
      required: [source, priority, category, subject, description, contactName]
    UpdateServiceRequestRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        customerId:
          type: [string, "null"]
          format: uuid
        locationId:
          type: [string, "null"]
          format: uuid
        assetId:
          type: [string, "null"]
          format: uuid
        priority:
          $ref: "#/components/schemas/Priority"
        category:
          type: string
          minLength: 1
          maxLength: 100
        subject:
          description: Canonical short summary of the request.
          type: string
          minLength: 1
          maxLength: 200
        title:
          description: Deprecated alias of subject, retained for backwards compatibility.
          deprecated: true
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: string
          minLength: 1
          maxLength: 5000
        contactName:
          type: string
          minLength: 1
          maxLength: 200
        contactPhone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
        preferredWindows:
          type: array
          maxItems: 5
          items:
            $ref: "#/components/schemas/PreferredTimeWindow"
    PilotUpdateServiceRequestSummaryRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      description: >-
        Content-only M3 summary patch. Status, internal note, original submission
        and customer bindings are intentionally excluded; title is a deprecated
        HTTP alias normalized to subject.
      properties:
        subject:
          type: string
          minLength: 1
          maxLength: 160
        title:
          description: Deprecated alias normalized to subject.
          deprecated: true
          type: string
          minLength: 1
          maxLength: 160
        description:
          type: string
          maxLength: 10000
        priority:
          anyOf:
            - $ref: "#/components/schemas/Priority"
            - type: "null"
        category:
          type: [string, "null"]
          minLength: 1
          maxLength: 100
        contactName:
          type: string
          minLength: 1
          maxLength: 120
        contactPhone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
    TriageServiceRequestRequest:
      type: object
      additionalProperties: false
      description: >-
        Binds request context and moves new to triaged. customerId is required
        by the triage RPC (TRIAGE_REQUIRES_CUSTOMER); locationId/assetId must be
        scoped under the same customer. All other fields are optional overrides
        applied during triage.
      properties:
        customerId:
          $ref: "#/components/schemas/Uuid"
        locationId:
          type: [string, "null"]
          format: uuid
        assetId:
          type: [string, "null"]
          format: uuid
        assignedMemberId:
          type: [string, "null"]
          format: uuid
        priority:
          $ref: "#/components/schemas/Priority"
        category:
          type: string
          minLength: 1
          maxLength: 100
        internalNote:
          type: [string, "null"]
          maxLength: 2000
      required: [customerId]
    MarkQuotedRequest:
      type: object
      additionalProperties: false
      properties:
        quoteId:
          $ref: "#/components/schemas/Uuid"
      required: [quoteId]
    ConvertWorkOrderInput:
      type: object
      additionalProperties: false
      description: Optional first work order created during conversion.
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 200
        scheduledStartAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        scheduledEndAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
      required: [title]
    ConvertServiceRequestRequest:
      type: object
      additionalProperties: false
      description: >-
        Atomically converts a request. mode=singleVisit creates only a work
        order; mode=project creates a project and optionally an initial work
        order. workOrder is the single canonical input for both modes.
      properties:
        mode:
          type: string
          enum: [singleVisit, project]
        projectTitle:
          type: string
          minLength: 1
          maxLength: 200
        workOrder:
          $ref: "#/components/schemas/ConvertWorkOrderInput"
      required: [mode]
    ConversionEnvelope:
      type: object
      additionalProperties: false
      description: >-
        Result of a conversion. project is null for singleVisit mode; workOrder
        is null when no initial work order was created for a project.
      properties:
        serviceRequest:
          $ref: "#/components/schemas/ServiceRequest"
        project:
          anyOf:
            - $ref: "#/components/schemas/Project"
            - type: "null"
        workOrder:
          anyOf:
            - $ref: "#/components/schemas/WorkOrder"
            - type: "null"
      required: [serviceRequest, project, workOrder]
    ServiceRequestEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/ServiceRequest"
      required: [data]
    ServiceRequestListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/ServiceRequest"
        meta:
          $ref: "#/components/schemas/ListMeta"
      required: [data, meta]

    PilotInboxItem:
      type: object
      additionalProperties: false
      description: Exact M3 keyset inbox row returned by the implemented list route.
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        referenceNo:
          type: string
          minLength: 1
          maxLength: 40
        source:
          type: string
          const: web
        contactName:
          type: string
          minLength: 1
          maxLength: 120
        contactPhone:
          $ref: "#/components/schemas/E164Phone"
        serviceName:
          type: [string, "null"]
          maxLength: 120
        category:
          type: [string, "null"]
          maxLength: 120
        title:
          type: string
          minLength: 1
          maxLength: 160
        description:
          type: string
          maxLength: 10000
        address:
          type: [string, "null"]
          maxLength: 1000
        photoCount:
          type: integer
          minimum: 0
        status:
          $ref: "#/components/schemas/ServiceRequestStatus"
        priority:
          $ref: "#/components/schemas/Priority"
        lockVersion:
          $ref: "#/components/schemas/LockVersion"
        customerId:
          type: [string, "null"]
          format: uuid
        assignedMemberId:
          type: [string, "null"]
          format: uuid
        triagedAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, referenceNo, source, contactName, contactPhone, serviceName, category, title, description, address, photoCount, status, priority, lockVersion, customerId, assignedMemberId, triagedAt, createdAt]
    PilotInboxListMeta:
      type: object
      additionalProperties: false
      properties:
        hasMore:
          type: boolean
        nextCursor:
          type: [string, "null"]
          maxLength: 1024
      required: [hasMore, nextCursor]
    PilotInboxListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/PilotInboxItem"
        meta:
          $ref: "#/components/schemas/PilotInboxListMeta"
      required: [data, meta]
    PilotOriginalSubmission:
      type: [object, "null"]
      description: Immutable staff-only snapshot captured when the customer submitted intake.
      additionalProperties: true
    PilotServiceRequestPhoto:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        category:
          type: string
          minLength: 1
          maxLength: 40
        url:
          type: string
          format: uri
        expiresAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, category, url, expiresAt]
    PilotServiceRequestPhotoListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          maxItems: 50
          items:
            $ref: "#/components/schemas/PilotServiceRequestPhoto"
      required: [data]
    PilotServiceRequestDetail:
      type: object
      additionalProperties: false
      description: Exact manager-only M3 detail workspace DTO.
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        requestNo:
          type: string
          minLength: 1
          maxLength: 40
        customerId:
          type: [string, "null"]
          format: uuid
        locationId:
          type: [string, "null"]
          format: uuid
        assetId:
          type: [string, "null"]
          format: uuid
        source:
          $ref: "#/components/schemas/ServiceRequestSource"
        status:
          $ref: "#/components/schemas/ServiceRequestStatus"
        priority:
          $ref: "#/components/schemas/Priority"
        category:
          type: [string, "null"]
          maxLength: 100
        subject:
          type: string
          minLength: 1
          maxLength: 160
        title:
          description: Deprecated alias that always mirrors subject.
          deprecated: true
          type: string
          minLength: 1
          maxLength: 160
        description:
          type: string
          maxLength: 10000
        contactName:
          type: string
          minLength: 1
          maxLength: 120
        contactPhone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
        contactEmail:
          type: [string, "null"]
          format: email
          maxLength: 254
        assignedMemberId:
          type: [string, "null"]
          format: uuid
        internalNote:
          type: string
          maxLength: 2000
        preferredWindows:
          type: array
          maxItems: 5
          items:
            $ref: "#/components/schemas/PreferredTimeWindow"
        originalSubmission:
          $ref: "#/components/schemas/PilotOriginalSubmission"
        summaryEditedBy:
          type: [string, "null"]
          format: uuid
        summaryEditedAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        triagedAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        convertedAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        convertedProjectId:
          type: [string, "null"]
          format: uuid
        convertedWorkOrderId:
          type: [string, "null"]
          format: uuid
        convertedProjectNo:
          type: [string, "null"]
          maxLength: 40
        convertedWorkOrderNo:
          type: [string, "null"]
          maxLength: 40
        declineReason:
          type: [string, "null"]
          maxLength: 2000
        cancellationReason:
          type: [string, "null"]
          maxLength: 2000
        photos:
          type: array
          maxItems: 50
          items:
            $ref: "#/components/schemas/PilotServiceRequestPhoto"
        lockVersion:
          $ref: "#/components/schemas/LockVersion"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, requestNo, customerId, locationId, assetId, source, status, priority, category, subject, title, description, contactName, contactPhone, contactEmail, assignedMemberId, internalNote, preferredWindows, originalSubmission, summaryEditedBy, summaryEditedAt, triagedAt, convertedAt, convertedProjectId, convertedWorkOrderId, convertedProjectNo, convertedWorkOrderNo, declineReason, cancellationReason, photos, lockVersion, createdAt, updatedAt]
    PilotServiceRequestDetailEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/PilotServiceRequestDetail"
      required: [data]
    PilotServiceRequestActionResult:
      type: object
      additionalProperties: false
      description: Compact result returned by triage and status transition actions.
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        status:
          $ref: "#/components/schemas/ServiceRequestStatus"
        priority:
          $ref: "#/components/schemas/Priority"
        category:
          type: [string, "null"]
          maxLength: 100
        customerId:
          type: [string, "null"]
          format: uuid
        locationId:
          type: [string, "null"]
          format: uuid
        assetId:
          type: [string, "null"]
          format: uuid
        assignedMemberId:
          type: [string, "null"]
          format: uuid
        triagedAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        convertedAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        convertedProjectId:
          type: [string, "null"]
          format: uuid
        convertedWorkOrderId:
          type: [string, "null"]
          format: uuid
        lockVersion:
          $ref: "#/components/schemas/LockVersion"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, status, priority, category, customerId, locationId, assetId, assignedMemberId, triagedAt, convertedAt, convertedProjectId, convertedWorkOrderId, lockVersion, updatedAt]
    PilotServiceRequestActionEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/PilotServiceRequestActionResult"
      required: [data]
    PilotConversionServiceRequest:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        status:
          type: string
          const: converted
        lockVersion:
          $ref: "#/components/schemas/LockVersion"
        convertedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, status, lockVersion, convertedAt]
    PilotConversionProject:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        projectNo:
          type: string
          minLength: 1
          maxLength: 40
        name:
          type: string
          minLength: 1
          maxLength: 200
        status:
          type: string
          minLength: 1
          maxLength: 40
      required: [id, projectNo, name, status]
    PilotConversionWorkOrder:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        workOrderNo:
          type: string
          minLength: 1
          maxLength: 40
        title:
          type: string
          minLength: 1
          maxLength: 200
        status:
          type: string
          minLength: 1
          maxLength: 40
      required: [id, workOrderNo, title, status]
    PilotConversionResult:
      type: object
      additionalProperties: false
      properties:
        serviceRequest:
          $ref: "#/components/schemas/PilotConversionServiceRequest"
        project:
          anyOf:
            - $ref: "#/components/schemas/PilotConversionProject"
            - type: "null"
        workOrder:
          anyOf:
            - $ref: "#/components/schemas/PilotConversionWorkOrder"
            - type: "null"
        replayed:
          type: boolean
      required: [serviceRequest, project, workOrder, replayed]
    PilotConversionEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/PilotConversionResult"
      required: [data]

    PublicIntakeConfiguration:
      type: object
      additionalProperties: false
      properties:
        merchantName:
          type: string
          maxLength: 200
        headline:
          type: string
          maxLength: 160
        serviceCatalogItems:
          type: array
          maxItems: 50
          items:
            type: object
            additionalProperties: false
            properties:
              id:
                $ref: "#/components/schemas/Uuid"
              name:
                type: string
                minLength: 1
                maxLength: 120
              category:
                type: string
                minLength: 1
                maxLength: 120
            required: [id, name, category]
        photoLimit:
          type: integer
          const: 3
        acceptedPhotoTypes:
          type: array
          items:
            type: string
            enum: [image/jpeg, image/png, image/webp]
        privacyNotice:
          type: string
          maxLength: 2000
      required: [merchantName, headline, serviceCatalogItems, photoLimit, acceptedPhotoTypes, privacyNotice]
    PublicIntakeEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/PublicIntakeConfiguration"
      required: [data]
    PublicServiceRequestRequest:
      type: object
      additionalProperties: false
      properties:
        submissionId:
          $ref: "#/components/schemas/Uuid"
        contactName:
          type: string
          minLength: 1
          maxLength: 120
        contactPhone:
          type: string
          maxLength: 40
          description: Taiwan local (09xx...) or E.164 input; normalized to E.164 server-side.
        serviceCatalogItemId:
          $ref: "#/components/schemas/Uuid"
        title:
          type: string
          minLength: 1
          maxLength: 160
        description:
          type: string
          minLength: 1
          maxLength: 10000
        address:
          type: object
          additionalProperties: false
          properties:
            postalCode:
              type: string
              minLength: 3
              maxLength: 10
            county:
              type: string
              minLength: 1
              maxLength: 40
            district:
              type: string
              minLength: 1
              maxLength: 40
            addressLine:
              type: string
              minLength: 1
              maxLength: 300
            accessNotes:
              type: string
              maxLength: 500
          required: [addressLine]
        preferredWindows:
          type: array
          maxItems: 5
          items:
            type: object
            additionalProperties: false
            properties:
              startsAt:
                $ref: "#/components/schemas/Timestamp"
              endsAt:
                $ref: "#/components/schemas/Timestamp"
              preferenceRank:
                type: integer
                minimum: 1
                maximum: 5
            required: [startsAt, endsAt, preferenceRank]
        photoIds:
          type: array
          uniqueItems: true
          maxItems: 3
          items:
            $ref: "#/components/schemas/Uuid"
        privacyAccepted:
          type: boolean
          const: true
        companyWebsite:
          type: string
          const: ""
          description: Honeypot — must be submitted as the empty string.
      required: [submissionId, contactName, contactPhone, serviceCatalogItemId, title, description, address, preferredWindows, photoIds, privacyAccepted, companyWebsite]
    PublicServiceRequestAccepted:
      type: object
      additionalProperties: false
      properties:
        referenceNo:
          type: string
          maxLength: 40
        receivedAt:
          $ref: "#/components/schemas/Timestamp"
        message:
          type: string
          maxLength: 500
      required: [referenceNo, receivedAt, message]
    PublicServiceRequestAcceptedEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/PublicServiceRequestAccepted"
      required: [data]

    Project:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        projectNo:
          type: string
          maxLength: 40
        customerId:
          $ref: "#/components/schemas/Uuid"
        locationId:
          $ref: "#/components/schemas/Uuid"
        serviceRequestId:
          type: [string, "null"]
          format: uuid
        title:
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: [string, "null"]
          maxLength: 5000
        status:
          $ref: "#/components/schemas/ProjectStatus"
        plannedStartOn:
          anyOf:
            - $ref: "#/components/schemas/BusinessDate"
            - type: "null"
        plannedEndOn:
          anyOf:
            - $ref: "#/components/schemas/BusinessDate"
            - type: "null"
        completedAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        lockVersion:
          $ref: "#/components/schemas/LockVersion"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, projectNo, customerId, locationId, serviceRequestId, title, description, status, plannedStartOn, plannedEndOn, completedAt, lockVersion, createdAt, updatedAt]
    CreateProjectRequest:
      type: object
      additionalProperties: false
      properties:
        customerId:
          $ref: "#/components/schemas/Uuid"
        locationId:
          $ref: "#/components/schemas/Uuid"
        serviceRequestId:
          type: [string, "null"]
          format: uuid
        title:
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: [string, "null"]
          maxLength: 5000
        plannedStartOn:
          anyOf:
            - $ref: "#/components/schemas/BusinessDate"
            - type: "null"
        plannedEndOn:
          anyOf:
            - $ref: "#/components/schemas/BusinessDate"
            - type: "null"
      required: [customerId, locationId, title]
    UpdateProjectRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: [string, "null"]
          maxLength: 5000
        plannedStartOn:
          anyOf:
            - $ref: "#/components/schemas/BusinessDate"
            - type: "null"
        plannedEndOn:
          anyOf:
            - $ref: "#/components/schemas/BusinessDate"
            - type: "null"
    CompleteProjectRequest:
      type: object
      additionalProperties: false
      properties:
        overrideReason:
          type: [string, "null"]
          maxLength: 2000
      required: [overrideReason]
    ProjectEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/Project"
      required: [data]
    ProjectListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Project"
        meta:
          $ref: "#/components/schemas/ListMeta"
      required: [data, meta]
    ProjectSummary:
      type: object
      additionalProperties: false
      properties:
        project:
          $ref: "#/components/schemas/Project"
        workOrderCounts:
          $ref: "#/components/schemas/StatusCountList"
        quoteTotalMinor:
          $ref: "#/components/schemas/MoneyMinor"
        acceptedChangeOrderNetMinor:
          $ref: "#/components/schemas/SignedMoneyMinor"
        paidMinor:
          $ref: "#/components/schemas/MoneyMinor"
        outstandingMinor:
          $ref: "#/components/schemas/MoneyMinor"
        currency:
          $ref: "#/components/schemas/Currency"
      required: [project, workOrderCounts, quoteTotalMinor, acceptedChangeOrderNetMinor, paidMinor, outstandingMinor, currency]
    ProjectSummaryEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/ProjectSummary"
      required: [data]
    StatusCount:
      type: object
      additionalProperties: false
      properties:
        status:
          type: string
          maxLength: 64
        count:
          type: integer
          minimum: 0
      required: [status, count]
    StatusCountList:
      type: array
      items:
        $ref: "#/components/schemas/StatusCount"

    WorkOrderDetailAssignment:
      type: object
      additionalProperties: false
      description: Assignment roster entry as projected onto the work-order detail.
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        membershipId:
          $ref: "#/components/schemas/Uuid"
        memberName:
          type: [string, "null"]
        duty:
          type: string
        status:
          type: string
        assignedAt:
          type: [string, "null"]
        acceptedAt:
          type: [string, "null"]
        declinedAt:
          type: [string, "null"]
        checkedInAt:
          type: [string, "null"]
        completedAt:
          type: [string, "null"]
        cancelledAt:
          type: [string, "null"]
        declineReason:
          type: [string, "null"]
        lockVersion:
          type: integer
      required: [id, membershipId, memberName, duty, status, assignedAt, acceptedAt, declinedAt, checkedInAt, completedAt, cancelledAt, declineReason, lockVersion]
    WorkOrderDetailChecklistItem:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        label:
          type: string
        responseType:
          type: string
        isRequired:
          type: boolean
        evidenceRequired:
          type: boolean
        options:
          description: Opaque option payload; shape depends on responseType.
        response:
          description: Opaque recorded response; shape depends on responseType.
        completedAt:
          type: [string, "null"]
        completedByMembershipId:
          type: [string, "null"]
          format: uuid
        sortOrder:
          type: integer
      required: [id, label, responseType, isRequired, evidenceRequired, options, response, completedAt, completedByMembershipId, sortOrder]
    WorkOrderDetailChecklist:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        name:
          type: string
        status:
          type: string
        completedAt:
          type: [string, "null"]
        completedByMembershipId:
          type: [string, "null"]
          format: uuid
        lockVersion:
          type: integer
        items:
          type: array
          items:
            $ref: "#/components/schemas/WorkOrderDetailChecklistItem"
      required: [id, name, status, completedAt, completedByMembershipId, lockVersion, items]
    WorkOrderDetailPhoto:
      type: object
      additionalProperties: false
      description: >-
        Photo projection. storagePath is emitted empty to clients; photo bytes are
        read only through the authorized signed-URL route.
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        category:
          type: string
        status:
          type: string
        checklistItemId:
          type: [string, "null"]
          format: uuid
        storagePath:
          type: string
        mimeType:
          type: string
        byteSize:
          type: [integer, "null"]
        width:
          type: [integer, "null"]
        height:
          type: [integer, "null"]
        sha256:
          type: [string, "null"]
        caption:
          type: [string, "null"]
        capturedAt:
          type: [string, "null"]
        uploadedByMembershipId:
          type: [string, "null"]
          format: uuid
        readyAt:
          type: [string, "null"]
        lockVersion:
          type: integer
        createdAt:
          type: string
      required: [id, category, status, checklistItemId, storagePath, mimeType, byteSize, width, height, sha256, caption, capturedAt, uploadedByMembershipId, readyAt, lockVersion, createdAt]
    WorkOrder:
      type: object
      additionalProperties: false
      description: >-
        Work-order list summary returned by the collection and schedule list
        endpoints. The single-resource detail projection is WorkOrderDetail.
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        workOrderNo:
          type: string
          maxLength: 40
        projectId:
          type: [string, "null"]
          format: uuid
        serviceRequestId:
          type: [string, "null"]
          format: uuid
        customerId:
          $ref: "#/components/schemas/Uuid"
        locationId:
          $ref: "#/components/schemas/Uuid"
        assetId:
          type: [string, "null"]
          format: uuid
        title:
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: [string, "null"]
          maxLength: 5000
        status:
          $ref: "#/components/schemas/WorkOrderStatus"
        priority:
          $ref: "#/components/schemas/Priority"
        scheduledStartAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        scheduledEndAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        completedAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        customerNotes:
          type: [string, "null"]
          maxLength: 2000
        technicianNotes:
          type: [string, "null"]
          maxLength: 5000
        lockVersion:
          $ref: "#/components/schemas/LockVersion"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, workOrderNo, projectId, serviceRequestId, customerId, locationId, assetId, title, description, status, priority, scheduledStartAt, scheduledEndAt, completedAt, customerNotes, technicianNotes, lockVersion, createdAt, updatedAt]
    WorkOrderDetail:
      type: object
      additionalProperties: false
      description: >-
        Full work-order detail projection returned by create, get, schedule,
        transition and force-complete. Includes the nested assignment roster,
        checklist snapshots and photo projections.
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        organizationId:
          $ref: "#/components/schemas/Uuid"
        workOrderNo:
          type: string
        projectId:
          type: [string, "null"]
          format: uuid
        serviceRequestId:
          type: [string, "null"]
          format: uuid
        customerId:
          $ref: "#/components/schemas/Uuid"
        locationId:
          $ref: "#/components/schemas/Uuid"
        assetId:
          type: [string, "null"]
          format: uuid
        title:
          type: string
        description:
          type: [string, "null"]
        customerNotes:
          type: [string, "null"]
        technicianNotes:
          type: [string, "null"]
        internalNotes:
          type: [string, "null"]
        completionSummary:
          type: [string, "null"]
        priority:
          type: string
        status:
          type: string
        scheduledStartAt:
          type: [string, "null"]
        scheduledEndAt:
          type: [string, "null"]
        dispatchedAt:
          type: [string, "null"]
        enRouteAt:
          type: [string, "null"]
        onSiteAt:
          type: [string, "null"]
        pausedAt:
          type: [string, "null"]
        completedAt:
          type: [string, "null"]
        cancelledAt:
          type: [string, "null"]
        cancellationReason:
          type: [string, "null"]
        requiresCustomerSignoff:
          type: boolean
        customerSignedAt:
          type: [string, "null"]
        lockVersion:
          type: integer
        createdAt:
          type: string
        updatedAt:
          type: string
        assignments:
          type: array
          items:
            $ref: "#/components/schemas/WorkOrderDetailAssignment"
        checklists:
          type: array
          items:
            $ref: "#/components/schemas/WorkOrderDetailChecklist"
        photos:
          type: array
          items:
            $ref: "#/components/schemas/WorkOrderDetailPhoto"
        replayed:
          type: boolean
      required: [id, organizationId, workOrderNo, projectId, serviceRequestId, customerId, locationId, assetId, title, description, customerNotes, technicianNotes, internalNotes, completionSummary, priority, status, scheduledStartAt, scheduledEndAt, dispatchedAt, enRouteAt, onSiteAt, pausedAt, completedAt, cancelledAt, cancellationReason, requiresCustomerSignoff, customerSignedAt, lockVersion, createdAt, updatedAt, assignments, checklists, photos]
    CreateWorkOrderRequest:
      type: object
      additionalProperties: false
      properties:
        projectId:
          type: [string, "null"]
          format: uuid
        serviceRequestId:
          type: [string, "null"]
          format: uuid
        customerId:
          $ref: "#/components/schemas/Uuid"
        locationId:
          $ref: "#/components/schemas/Uuid"
        assetId:
          type: [string, "null"]
          format: uuid
        title:
          type: string
          minLength: 1
          maxLength: 160
        description:
          type: [string, "null"]
          maxLength: 5000
        priority:
          $ref: "#/components/schemas/Priority"
        customerNotes:
          type: [string, "null"]
          maxLength: 2000
        scheduledStartAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        scheduledEndAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
      required: [projectId, serviceRequestId, customerId, locationId, title, priority, scheduledStartAt, scheduledEndAt]
    UpdateWorkOrderRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        assetId:
          type: [string, "null"]
          format: uuid
        title:
          type: string
          minLength: 1
          maxLength: 160
        description:
          type: [string, "null"]
          maxLength: 5000
        priority:
          $ref: "#/components/schemas/Priority"
        customerNotes:
          type: [string, "null"]
          maxLength: 2000
        technicianNotes:
          type: [string, "null"]
          maxLength: 5000
    ScheduleAssignmentInput:
      type: object
      additionalProperties: false
      properties:
        membershipId:
          $ref: "#/components/schemas/Uuid"
        duty:
          $ref: "#/components/schemas/AssignmentDuty"
      required: [membershipId]
    ScheduleWorkOrderRequest:
      type: object
      additionalProperties: false
      properties:
        scheduledStartAt:
          $ref: "#/components/schemas/Timestamp"
        scheduledEndAt:
          $ref: "#/components/schemas/Timestamp"
        occurredAt:
          $ref: "#/components/schemas/Timestamp"
        assignments:
          type: array
          minItems: 1
          maxItems: 20
          items:
            $ref: "#/components/schemas/ScheduleAssignmentInput"
        conflictOverrideReason:
          type: [string, "null"]
          minLength: 1
          maxLength: 2000
      required: [scheduledStartAt, scheduledEndAt, occurredAt, assignments]
    TransitionWorkOrderRequest:
      oneOf:
        - type: object
          additionalProperties: false
          properties:
            action:
              type: string
              enum: [dispatch, enRoute, arrive, pause, resume]
            occurredAt:
              $ref: "#/components/schemas/Timestamp"
            overrideReason:
              type: [string, "null"]
              minLength: 1
              maxLength: 2000
          required: [action, occurredAt]
        - type: object
          additionalProperties: false
          properties:
            action:
              type: string
              enum: [complete]
            occurredAt:
              $ref: "#/components/schemas/Timestamp"
            completionSummary:
              type: string
              minLength: 1
              maxLength: 10000
            customerSignoffName:
              type: [string, "null"]
              minLength: 1
              maxLength: 120
            overrideReason:
              type: [string, "null"]
              minLength: 1
              maxLength: 2000
          required: [action, occurredAt, completionSummary, customerSignoffName]
        - type: object
          additionalProperties: false
          properties:
            action:
              type: string
              enum: [cancel]
            occurredAt:
              $ref: "#/components/schemas/Timestamp"
            reason:
              type: string
              minLength: 1
              maxLength: 2000
            overrideReason:
              type: [string, "null"]
              minLength: 1
              maxLength: 2000
          required: [action, occurredAt, reason]
        - type: object
          additionalProperties: false
          properties:
            action:
              type: string
              enum: [reopen]
            occurredAt:
              $ref: "#/components/schemas/Timestamp"
            reason:
              type: string
              minLength: 1
              maxLength: 2000
            overrideReason:
              type: [string, "null"]
              minLength: 1
              maxLength: 2000
          required: [action, occurredAt, reason]
      discriminator:
        propertyName: action
    WorkOrderDetailEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/WorkOrderDetail"
      required: [data]
    MutationNotificationStatus:
      type: string
      description: >-
        Honest customer/technician notification status for M5 mutation responses.
        LINE delivery is deferred to M6, so scheduling, dispatch and completion
        never send a message and always report not_sent.
      enum: [not_sent]
    WorkOrderMutationNotification:
      type: object
      additionalProperties: false
      properties:
        status:
          $ref: "#/components/schemas/MutationNotificationStatus"
        reason:
          type: string
          description: Machine-readable reason the notification was not sent.
          maxLength: 200
      required: [status, reason]
    WorkOrderMutationEnvelope:
      type: object
      additionalProperties: false
      description: >-
        Work-order mutation response. Carries the updated detail plus the honest
        notification status, so the UI never implies a customer message was sent.
      properties:
        data:
          $ref: "#/components/schemas/WorkOrderDetail"
        notification:
          $ref: "#/components/schemas/WorkOrderMutationNotification"
      required: [data, notification]
    WorkOrderListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/WorkOrder"
        meta:
          $ref: "#/components/schemas/ListMeta"
      required: [data, meta]

    Assignment:
      type: object
      additionalProperties: false
      description: Assignment resource returned by create, update, cancel and respond.
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        organizationId:
          $ref: "#/components/schemas/Uuid"
        workOrderId:
          $ref: "#/components/schemas/Uuid"
        membershipId:
          $ref: "#/components/schemas/Uuid"
        duty:
          type: string
        status:
          type: string
        assignedAt:
          type: [string, "null"]
        acceptedAt:
          type: [string, "null"]
        declinedAt:
          type: [string, "null"]
        checkedInAt:
          type: [string, "null"]
        completedAt:
          type: [string, "null"]
        cancelledAt:
          type: [string, "null"]
        declineReason:
          type: [string, "null"]
        lockVersion:
          type: integer
      required: [id, organizationId, workOrderId, membershipId, duty, status, assignedAt, acceptedAt, declinedAt, checkedInAt, completedAt, cancelledAt, declineReason, lockVersion]
    CreateAssignmentRequest:
      type: object
      additionalProperties: false
      properties:
        membershipId:
          $ref: "#/components/schemas/Uuid"
        duty:
          $ref: "#/components/schemas/AssignmentDuty"
      required: [membershipId, duty]
    UpdateAssignmentRequest:
      type: object
      additionalProperties: false
      properties:
        duty:
          $ref: "#/components/schemas/AssignmentDuty"
      required: [duty]
    CancelAssignmentRequest:
      type: object
      additionalProperties: false
      description: Reason is mandatory and carried in the body because DELETE has no natural payload.
      properties:
        reason:
          type: string
          minLength: 1
          maxLength: 2000
      required: [reason]
    RespondAssignmentRequest:
      type: object
      additionalProperties: false
      properties:
        decision:
          type: string
          enum: [accept, decline]
        reason:
          type: [string, "null"]
          maxLength: 1000
      required: [decision]
    AssignmentEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/Assignment"
      required: [data]
    AssignmentListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Assignment"
        meta:
          $ref: "#/components/schemas/ListMeta"
      required: [data, meta]
    CheckScheduleConflictsRequest:
      type: object
      additionalProperties: false
      properties:
        membershipIds:
          type: array
          minItems: 1
          maxItems: 50
          uniqueItems: true
          items:
            $ref: "#/components/schemas/Uuid"
        startsAt:
          $ref: "#/components/schemas/Timestamp"
        endsAt:
          $ref: "#/components/schemas/Timestamp"
        excludeWorkOrderId:
          type: [string, "null"]
          format: uuid
      required: [membershipIds, startsAt, endsAt]
    ScheduleConflict:
      type: object
      additionalProperties: false
      properties:
        membershipId:
          $ref: "#/components/schemas/Uuid"
        workOrderId:
          $ref: "#/components/schemas/Uuid"
        workOrderNo:
          type: string
        startsAt:
          type: [string, "null"]
        endsAt:
          type: [string, "null"]
      required: [membershipId, workOrderId, workOrderNo, startsAt, endsAt]
    ScheduleConflictResult:
      type: object
      additionalProperties: false
      properties:
        conflicts:
          type: array
          items:
            $ref: "#/components/schemas/ScheduleConflict"
      required: [conflicts]
    ScheduleConflictEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/ScheduleConflictResult"
      required: [data]

    ChecklistTemplateItem:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        label:
          type: string
          minLength: 1
          maxLength: 500
        description:
          type: [string, "null"]
          maxLength: 2000
        itemType:
          $ref: "#/components/schemas/ChecklistItemType"
        required:
          type: boolean
        evidenceRequired:
          type: boolean
        options:
          type: array
          maxItems: 30
          items:
            type: string
            maxLength: 200
        sortOrder:
          type: integer
          minimum: 0
          maximum: 100000
      required: [id, label, description, itemType, required, evidenceRequired, options, sortOrder]
    ChecklistTemplate:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        name:
          type: string
          minLength: 1
          maxLength: 200
        category:
          type: [string, "null"]
          maxLength: 100
        active:
          type: boolean
        version:
          type: integer
          minimum: 1
        items:
          type: array
          maxItems: 200
          items:
            $ref: "#/components/schemas/ChecklistTemplateItem"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, name, category, active, version, items, createdAt, updatedAt]
    ChecklistTemplateItemInput:
      type: object
      additionalProperties: false
      properties:
        label:
          type: string
          minLength: 1
          maxLength: 500
        description:
          type: [string, "null"]
          maxLength: 2000
        itemType:
          $ref: "#/components/schemas/ChecklistItemType"
        required:
          type: boolean
          default: false
        evidenceRequired:
          type: boolean
          default: false
        options:
          type: array
          maxItems: 30
          items:
            type: string
            maxLength: 200
        sortOrder:
          type: integer
          minimum: 0
          maximum: 100000
      required: [label, itemType, required, evidenceRequired, options, sortOrder]
    CreateChecklistTemplateRequest:
      type: object
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        category:
          type: [string, "null"]
          maxLength: 100
        items:
          type: array
          minItems: 1
          maxItems: 200
          items:
            $ref: "#/components/schemas/ChecklistTemplateItemInput"
      required: [name, items]
    UpdateChecklistTemplateRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        category:
          type: [string, "null"]
          maxLength: 100
        active:
          type: boolean
        items:
          type: array
          minItems: 1
          maxItems: 200
          items:
            $ref: "#/components/schemas/ChecklistTemplateItemInput"
    ChecklistTemplateEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/ChecklistTemplate"
      required: [data]
    ChecklistTemplateListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/ChecklistTemplate"
        meta:
          $ref: "#/components/schemas/ListMeta"
      required: [data, meta]
    ChecklistResponse:
      type: object
      additionalProperties: false
      properties:
        checked:
          type: [boolean, "null"]
        text:
          type: [string, "null"]
          maxLength: 5000
        number:
          type: [number, "null"]
        selectedOption:
          type: [string, "null"]
          maxLength: 200
        photoIds:
          type: array
          uniqueItems: true
          maxItems: 20
          items:
            $ref: "#/components/schemas/Uuid"
      required: [checked, text, number, selectedOption, photoIds]
    WorkOrderChecklistItem:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        label:
          type: string
          maxLength: 500
        itemType:
          $ref: "#/components/schemas/ChecklistItemType"
        required:
          type: boolean
        options:
          type: array
          maxItems: 100
          items:
            type: string
            maxLength: 200
        response:
          $ref: "#/components/schemas/ChecklistResponse"
        sortOrder:
          type: integer
          minimum: 0
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, label, itemType, required, options, response, sortOrder, updatedAt]
    WorkOrderChecklist:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        workOrderId:
          $ref: "#/components/schemas/Uuid"
        templateId:
          type: [string, "null"]
          format: uuid
        templateVersion:
          type: [integer, "null"]
          minimum: 1
        name:
          type: string
          maxLength: 200
        status:
          $ref: "#/components/schemas/ChecklistStatus"
        items:
          type: array
          maxItems: 200
          items:
            $ref: "#/components/schemas/WorkOrderChecklistItem"
        completedAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        completedByMembershipId:
          type: [string, "null"]
          format: uuid
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, workOrderId, templateId, templateVersion, name, status, items, completedAt, completedByMembershipId, createdAt, updatedAt]
    CreateWorkOrderChecklistRequest:
      type: object
      additionalProperties: false
      properties:
        templateId:
          type: [string, "null"]
          format: uuid
        name:
          type: [string, "null"]
          maxLength: 200
        items:
          type: array
          maxItems: 200
          items:
            $ref: "#/components/schemas/ChecklistTemplateItemInput"
      anyOf:
        - required: [templateId]
        - required: [name, items]
    UpdateChecklistItemResponseRequest:
      type: object
      additionalProperties: false
      properties:
        response:
          $ref: "#/components/schemas/ChecklistResponse"
      required: [response]
    WorkOrderChecklistEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/WorkOrderChecklist"
      required: [data]
    WorkOrderChecklistListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/WorkOrderChecklist"
      required: [data]

    Photo:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        aggregateType:
          type: string
          enum: [service_request, work_order]
        aggregateId:
          $ref: "#/components/schemas/Uuid"
        category:
          $ref: "#/components/schemas/PhotoCategory"
        status:
          $ref: "#/components/schemas/PhotoStatus"
        filename:
          type: string
          minLength: 1
          maxLength: 200
        contentType:
          type: string
          enum: [image/jpeg, image/png, image/webp]
        byteSize:
          type: integer
          minimum: 1
          maximum: 10485760
        width:
          type: [integer, "null"]
          minimum: 1
        height:
          type: [integer, "null"]
          minimum: 1
        caption:
          type: [string, "null"]
          maxLength: 1000
        capturedAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        checklistItemId:
          type: [string, "null"]
          format: uuid
        downloadUrl:
          type: [string, "null"]
          format: uri
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, aggregateType, aggregateId, category, status, filename, contentType, byteSize, width, height, caption, capturedAt, checklistItemId, downloadUrl, createdAt, updatedAt]
    PhotoUploadRequest:
      type: object
      additionalProperties: false
      properties:
        filename:
          type: string
          minLength: 1
          maxLength: 200
        contentType:
          type: string
          enum: [image/jpeg, image/png, image/webp]
        byteSize:
          type: integer
          minimum: 1
          maximum: 10485760
        sha256:
          type: string
          pattern: "^[a-f0-9]{64}$"
        category:
          $ref: "#/components/schemas/PhotoCategory"
        caption:
          type: [string, "null"]
          maxLength: 1000
        capturedAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        checklistItemId:
          type: [string, "null"]
          format: uuid
      required: [filename, contentType, byteSize, sha256, category, capturedAt, checklistItemId]
    PublicPhotoUploadRequest:
      type: object
      additionalProperties: false
      properties:
        submissionId:
          $ref: "#/components/schemas/Uuid"
        filename:
          type: string
          minLength: 1
          maxLength: 200
        contentType:
          type: string
          enum: [image/jpeg, image/png, image/webp]
        byteSize:
          type: integer
          minimum: 1
          maximum: 10485760
        sha256:
          type: string
          pattern: "^[a-f0-9]{64}$"
      required: [submissionId, filename, contentType, byteSize, sha256]
    PublicCompletePhotoUploadRequest:
      type: object
      additionalProperties: false
      properties:
        submissionId:
          $ref: "#/components/schemas/Uuid"
      required: [submissionId]
    PublicPhotoReady:
      type: object
      additionalProperties: false
      properties:
        photoId:
          $ref: "#/components/schemas/Uuid"
        status:
          type: string
          const: ready
      required: [photoId, status]
    PublicPhotoReadyEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/PublicPhotoReady"
      required: [data]
    CompletePhotoUploadRequest:
      type: object
      additionalProperties: false
      description: The server verifies the previously declared checksum and object metadata.
      maxProperties: 0
    UpdatePhotoRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        category:
          $ref: "#/components/schemas/PhotoCategory"
        caption:
          type: [string, "null"]
          maxLength: 1000
        takenAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
    PhotoUploadInstruction:
      type: object
      additionalProperties: false
      properties:
        photoId:
          $ref: "#/components/schemas/Uuid"
        upload:
          type: object
          additionalProperties: false
          properties:
            method:
              type: string
              const: PUT
            url:
              type: string
              format: uri
            headers:
              type: object
              description: Allowlisted upload headers generated for the storage provider.
              additionalProperties:
                type: string
            expiresAt:
              $ref: "#/components/schemas/Timestamp"
          required: [method, url, headers, expiresAt]
      required: [photoId, upload]
    PhotoEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/Photo"
      required: [data]
    PhotoUploadEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/PhotoUploadInstruction"
      required: [data]

    ServiceCatalog:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        name:
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: [string, "null"]
          maxLength: 2000
        active:
          type: boolean
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, name, description, active, createdAt, updatedAt]
    ServiceCatalogItem:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        catalogId:
          $ref: "#/components/schemas/Uuid"
        code:
          type: [string, "null"]
          maxLength: 100
        category:
          type: [string, "null"]
          maxLength: 100
        name:
          type: string
          minLength: 1
          maxLength: 300
        description:
          type: [string, "null"]
          maxLength: 2000
        unit:
          type: string
          minLength: 1
          maxLength: 30
        defaultCostMinor:
          $ref: "#/components/schemas/MoneyMinor"
        defaultPriceMinor:
          $ref: "#/components/schemas/MoneyMinor"
        taxRate:
          $ref: "#/components/schemas/TaxRate"
        active:
          type: boolean
        sortOrder:
          type: integer
          minimum: 0
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, catalogId, code, category, name, description, unit, defaultCostMinor, defaultPriceMinor, taxRate, active, sortOrder, createdAt, updatedAt]
    CreateServiceCatalogRequest:
      type: object
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: [string, "null"]
          maxLength: 2000
      required: [name]
    UpdateServiceCatalogRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: [string, "null"]
          maxLength: 2000
        active:
          type: boolean
    ServiceCatalogItemInput:
      type: object
      additionalProperties: false
      properties:
        code:
          type: [string, "null"]
          maxLength: 100
        category:
          type: [string, "null"]
          maxLength: 100
        name:
          type: string
          minLength: 1
          maxLength: 300
        description:
          type: [string, "null"]
          maxLength: 2000
        unit:
          type: string
          minLength: 1
          maxLength: 30
        defaultCostMinor:
          $ref: "#/components/schemas/MoneyMinor"
        defaultPriceMinor:
          $ref: "#/components/schemas/MoneyMinor"
        taxRate:
          $ref: "#/components/schemas/TaxRate"
        active:
          type: boolean
          default: true
        sortOrder:
          type: integer
          minimum: 0
      required: [name, unit, defaultCostMinor, defaultPriceMinor, taxRate, active, sortOrder]
    CreateServiceCatalogItemRequest:
      $ref: "#/components/schemas/ServiceCatalogItemInput"
    UpdateServiceCatalogItemRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        code:
          type: [string, "null"]
          maxLength: 100
        category:
          type: [string, "null"]
          maxLength: 100
        name:
          type: string
          minLength: 1
          maxLength: 300
        description:
          type: [string, "null"]
          maxLength: 2000
        unit:
          type: string
          minLength: 1
          maxLength: 30
        defaultCostMinor:
          $ref: "#/components/schemas/MoneyMinor"
        defaultPriceMinor:
          $ref: "#/components/schemas/MoneyMinor"
        taxRate:
          $ref: "#/components/schemas/TaxRate"
        active:
          type: boolean
        sortOrder:
          type: integer
          minimum: 0
    BulkUpsertCatalogItem:
      type: object
      additionalProperties: false
      properties:
        id:
          type: [string, "null"]
          format: uuid
        clientKey:
          type: string
          minLength: 1
          maxLength: 100
        item:
          $ref: "#/components/schemas/ServiceCatalogItemInput"
      required: [id, clientKey, item]
    BulkUpsertCatalogItemsRequest:
      type: object
      additionalProperties: false
      properties:
        items:
          type: array
          minItems: 1
          maxItems: 200
          items:
            $ref: "#/components/schemas/BulkUpsertCatalogItem"
      required: [items]
    BulkUpsertCatalogItemsResult:
      type: object
      additionalProperties: false
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/ServiceCatalogItem"
      required: [items]
    ServiceCatalogEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/ServiceCatalog"
      required: [data]
    ServiceCatalogListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/ServiceCatalog"
        meta:
          $ref: "#/components/schemas/ListMeta"
      required: [data, meta]
    ServiceCatalogItemEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/ServiceCatalogItem"
      required: [data]
    ServiceCatalogItemListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/ServiceCatalogItem"
        meta:
          $ref: "#/components/schemas/ListMeta"
      required: [data, meta]
    BulkUpsertCatalogItemsEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/BulkUpsertCatalogItemsResult"
      required: [data]

    QuoteItemInput:
      type: object
      additionalProperties: false
      properties:
        serviceCatalogItemId:
          type: [string, "null"]
          format: uuid
        groupName:
          type: string
          maxLength: 120
        name:
          type: string
          minLength: 1
          maxLength: 300
        specification:
          type: string
          maxLength: 5000
        unit:
          type: string
          minLength: 1
          maxLength: 20
        quantity:
          $ref: "#/components/schemas/Quantity"
        unitCostMinor:
          $ref: "#/components/schemas/MoneyMinor"
        unitPriceMinor:
          $ref: "#/components/schemas/MoneyMinor"
        discountMinor:
          $ref: "#/components/schemas/MoneyMinor"
        taxRate:
          $ref: "#/components/schemas/TaxRate"
        sortOrder:
          type: integer
          minimum: 0
          maximum: 100000
      required: [serviceCatalogItemId, groupName, name, specification, unit, quantity, unitCostMinor, unitPriceMinor, discountMinor, taxRate, sortOrder]
    QuoteItem:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        serviceCatalogItemId:
          type: [string, "null"]
          format: uuid
        groupName:
          type: string
          maxLength: 120
        name:
          type: string
          maxLength: 300
        specification:
          type: string
          maxLength: 5000
        unit:
          type: string
          maxLength: 20
        quantity:
          $ref: "#/components/schemas/Quantity"
        unitCostMinor:
          $ref: "#/components/schemas/MoneyMinor"
        unitPriceMinor:
          $ref: "#/components/schemas/MoneyMinor"
        discountMinor:
          $ref: "#/components/schemas/MoneyMinor"
        taxRate:
          $ref: "#/components/schemas/TaxRate"
        subtotalMinor:
          $ref: "#/components/schemas/MoneyMinor"
        taxMinor:
          $ref: "#/components/schemas/MoneyMinor"
        totalMinor:
          $ref: "#/components/schemas/MoneyMinor"
        sortOrder:
          type: integer
          minimum: 0
      required: [id, serviceCatalogItemId, groupName, name, specification, unit, quantity, unitCostMinor, unitPriceMinor, discountMinor, taxRate, subtotalMinor, taxMinor, totalMinor, sortOrder]
    QuoteVersionInput:
      type: object
      additionalProperties: false
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 160
        validUntil:
          anyOf:
            - $ref: "#/components/schemas/BusinessDate"
            - type: "null"
        customerNotes:
          type: string
          maxLength: 10000
        internalNotes:
          type: string
          maxLength: 10000
        terms:
          type: string
          maxLength: 10000
        items:
          type: array
          minItems: 1
          maxItems: 300
          items:
            $ref: "#/components/schemas/QuoteItemInput"
      required: [title, validUntil, customerNotes, internalNotes, terms, items]
    QuoteVersion:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        quoteId:
          $ref: "#/components/schemas/Uuid"
        versionNo:
          type: integer
          minimum: 1
        status:
          $ref: "#/components/schemas/QuoteVersionStatus"
        approvalStatus:
          $ref: "#/components/schemas/QuoteApprovalStatus"
        title:
          type: string
          maxLength: 160
        validUntil:
          anyOf:
            - $ref: "#/components/schemas/BusinessDate"
            - type: "null"
        customerNotes:
          type: [string, "null"]
          maxLength: 5000
        terms:
          type: [string, "null"]
          maxLength: 10000
        items:
          type: array
          maxItems: 300
          items:
            $ref: "#/components/schemas/QuoteItem"
        subtotalMinor:
          $ref: "#/components/schemas/MoneyMinor"
        discountMinor:
          $ref: "#/components/schemas/MoneyMinor"
        taxMinor:
          $ref: "#/components/schemas/MoneyMinor"
        totalMinor:
          $ref: "#/components/schemas/MoneyMinor"
        currency:
          $ref: "#/components/schemas/Currency"
        submittedForApprovalAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        approvedAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        approvedByMembershipId:
          type: [string, "null"]
          format: uuid
        approvalReason:
          type: [string, "null"]
          maxLength: 1000
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, quoteId, versionNo, status, approvalStatus, title, validUntil, customerNotes, terms, items, subtotalMinor, discountMinor, taxMinor, totalMinor, currency, submittedForApprovalAt, approvedAt, approvedByMembershipId, approvalReason, createdAt, updatedAt]
    Quote:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        quoteNo:
          type: string
          maxLength: 40
        serviceRequestId:
          type: [string, "null"]
          format: uuid
        projectId:
          type: [string, "null"]
          format: uuid
        customerId:
          $ref: "#/components/schemas/Uuid"
        locationId:
          $ref: "#/components/schemas/Uuid"
        status:
          $ref: "#/components/schemas/QuoteStatus"
        currency:
          $ref: "#/components/schemas/Currency"
        activeVersionId:
          type: [string, "null"]
          format: uuid
        latestVersionId:
          type: [string, "null"]
          format: uuid
        acceptedVersionId:
          type: [string, "null"]
          format: uuid
        activeVersion:
          anyOf:
            - $ref: "#/components/schemas/QuoteVersion"
            - type: "null"
        sentAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        respondedAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        expiresAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        cancellationReason:
          type: [string, "null"]
          maxLength: 2000
        lockVersion:
          $ref: "#/components/schemas/LockVersion"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, quoteNo, serviceRequestId, projectId, customerId, locationId, status, currency, latestVersionId, activeVersionId, acceptedVersionId, activeVersion, sentAt, respondedAt, expiresAt, cancellationReason, lockVersion, createdAt, updatedAt]
    CreateQuoteRequest:
      type: object
      additionalProperties: false
      properties:
        serviceRequestId:
          $ref: "#/components/schemas/Uuid"
        customerId:
          $ref: "#/components/schemas/Uuid"
        locationId:
          $ref: "#/components/schemas/Uuid"
        currency:
          $ref: "#/components/schemas/Currency"
        version:
          $ref: "#/components/schemas/QuoteVersionInput"
      required: [serviceRequestId, customerId, locationId, currency, version]
    UpdateQuoteRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        expiresAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        cancellationReason:
          type: [string, "null"]
          maxLength: 2000
    CreateQuoteVersionRequest:
      type: object
      additionalProperties: false
      properties:
        cloneFromVersionId:
          $ref: "#/components/schemas/Uuid"
      required: [cloneFromVersionId]
    UpdateQuoteVersionRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 160
        validUntil:
          anyOf:
            - $ref: "#/components/schemas/BusinessDate"
            - type: "null"
        customerNotes:
          type: [string, "null"]
          maxLength: 5000
        terms:
          type: [string, "null"]
          maxLength: 10000
    ReplaceQuoteItemsRequest:
      type: object
      additionalProperties: false
      properties:
        items:
          type: array
          minItems: 1
          maxItems: 300
          items:
            $ref: "#/components/schemas/QuoteItemInput"
      required: [items]
    SendQuoteRequest:
      type: object
      additionalProperties: false
      properties:
        versionId:
          $ref: "#/components/schemas/Uuid"
        serviceRequestLockVersion:
          $ref: "#/components/schemas/LockVersion"
      required: [versionId, serviceRequestLockVersion]
    PublicMerchant:
      type: object
      additionalProperties: false
      properties:
        name:
          type: string
          maxLength: 120
        phone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
      required: [name, phone]
    PublicQuoteItem:
      type: object
      additionalProperties: false
      properties:
        name:
          type: string
          maxLength: 300
        specification:
          type: [string, "null"]
          maxLength: 2000
        unit:
          type: string
          maxLength: 30
        quantity:
          $ref: "#/components/schemas/Quantity"
        unitPriceMinor:
          $ref: "#/components/schemas/MoneyMinor"
        discountMinor:
          $ref: "#/components/schemas/MoneyMinor"
        totalMinor:
          $ref: "#/components/schemas/MoneyMinor"
      required: [name, specification, unit, quantity, unitPriceMinor, discountMinor, totalMinor]
    PublicQuote:
      type: object
      additionalProperties: false
      properties:
        merchant:
          $ref: "#/components/schemas/PublicMerchant"
        quoteNo:
          type: string
          maxLength: 40
        versionNo:
          type: integer
          minimum: 1
        status:
          $ref: "#/components/schemas/QuoteStatus"
        validUntil:
          anyOf:
            - $ref: "#/components/schemas/BusinessDate"
            - type: "null"
        title:
          type: string
          minLength: 1
          maxLength: 160
        items:
          type: array
          items:
            $ref: "#/components/schemas/PublicQuoteItem"
        subtotalMinor:
          $ref: "#/components/schemas/MoneyMinor"
        discountMinor:
          $ref: "#/components/schemas/MoneyMinor"
        taxMinor:
          $ref: "#/components/schemas/MoneyMinor"
        totalMinor:
          $ref: "#/components/schemas/MoneyMinor"
        currency:
          $ref: "#/components/schemas/Currency"
        customerNotes:
          type: string
          maxLength: 10000
        terms:
          type: string
          maxLength: 10000
        decision:
          anyOf:
            - $ref: "#/components/schemas/PublicQuoteDecision"
            - type: "null"
      required: [merchant, quoteNo, versionNo, status, validUntil, title, items, subtotalMinor, discountMinor, taxMinor, totalMinor, currency, customerNotes, terms, decision]
    PublicQuoteResponseRequest:
      type: object
      additionalProperties: false
      properties:
        decision:
          $ref: "#/components/schemas/PublicDecision"
        displayName:
          type: string
          minLength: 1
          maxLength: 120
        comment:
          type: [string, "null"]
          maxLength: 2000
      required: [decision, displayName]
    PublicDecisionRecord:
      type: object
      additionalProperties: false
      properties:
        decision:
          $ref: "#/components/schemas/PublicDecision"
        recordedAt:
          $ref: "#/components/schemas/Timestamp"
        displayName:
          type: string
          maxLength: 120
        comment:
          type: [string, "null"]
          maxLength: 2000
        replayed:
          type: boolean
      required: [decision, recordedAt, displayName, comment, replayed]
    PublicQuoteDecision:
      type: object
      additionalProperties: false
      properties:
        decision:
          $ref: "#/components/schemas/PublicDecision"
        recordedAt:
          $ref: "#/components/schemas/Timestamp"
        displayName:
          type: string
          maxLength: 120
        comment:
          type: [string, "null"]
          maxLength: 2000
      required: [decision, recordedAt, displayName, comment]
    QuoteEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/Quote"
      required: [data]
    QuoteListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Quote"
        meta:
          $ref: "#/components/schemas/ListMeta"
      required: [data, meta]
    QuoteVersionEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/QuoteVersion"
      required: [data]
    PublicQuoteEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/PublicQuote"
      required: [data]
    PublicDecisionEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/PublicDecisionRecord"
      required: [data]
    PilotQuoteAggregate:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        quoteNo:
          type: string
          minLength: 1
          maxLength: 40
        serviceRequestId:
          $ref: "#/components/schemas/Uuid"
        customerId:
          $ref: "#/components/schemas/Uuid"
        locationId:
          $ref: "#/components/schemas/Uuid"
        status:
          $ref: "#/components/schemas/QuoteStatus"
        currency:
          type: string
          enum: [TWD]
        latestVersionId:
          $ref: "#/components/schemas/Uuid"
        activeVersionId:
          type: [string, "null"]
          format: uuid
        acceptedVersionId:
          type: [string, "null"]
          format: uuid
        sentAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        firstViewedAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        acceptedAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        rejectedAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        rejectionReason:
          type: [string, "null"]
        expiresAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        lockVersion:
          $ref: "#/components/schemas/LockVersion"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, quoteNo, serviceRequestId, customerId, locationId, status, currency, latestVersionId, activeVersionId, acceptedVersionId, sentAt, firstViewedAt, acceptedAt, rejectedAt, rejectionReason, expiresAt, lockVersion, createdAt, updatedAt]
    PilotQuoteVersion:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        quoteId:
          $ref: "#/components/schemas/Uuid"
        versionNo:
          type: integer
          minimum: 1
        status:
          $ref: "#/components/schemas/QuoteVersionStatus"
        approvalStatus:
          $ref: "#/components/schemas/QuoteApprovalStatus"
        title:
          type: string
          minLength: 1
          maxLength: 160
        validUntil:
          anyOf:
            - $ref: "#/components/schemas/BusinessDate"
            - type: "null"
        customerNotes:
          type: string
          maxLength: 10000
        internalNotes:
          type: string
          maxLength: 10000
        terms:
          type: string
          maxLength: 10000
        subtotalMinor:
          $ref: "#/components/schemas/MoneyMinor"
        discountMinor:
          $ref: "#/components/schemas/MoneyMinor"
        taxMinor:
          $ref: "#/components/schemas/MoneyMinor"
        totalMinor:
          $ref: "#/components/schemas/MoneyMinor"
        items:
          type: array
          maxItems: 300
          items:
            $ref: "#/components/schemas/QuoteItem"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, quoteId, versionNo, status, approvalStatus, title, validUntil, customerNotes, internalNotes, terms, subtotalMinor, discountMinor, taxMinor, totalMinor, items, createdAt, updatedAt]
    PilotQuoteRequestSummary:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        requestNo:
          type: string
          minLength: 1
          maxLength: 40
        subject:
          type: string
          minLength: 1
          maxLength: 160
        status:
          $ref: "#/components/schemas/ServiceRequestStatus"
        lockVersion:
          $ref: "#/components/schemas/LockVersion"
      required: [id, requestNo, subject, status, lockVersion]
    PilotQuoteCustomerSummary:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        name:
          type: string
          minLength: 1
          maxLength: 120
        phone:
          anyOf:
            - $ref: "#/components/schemas/E164Phone"
            - type: "null"
      required: [id, name, phone]
    PilotQuoteLocationSummary:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        label:
          type: string
          minLength: 1
          maxLength: 80
        address:
          type: string
          minLength: 1
          maxLength: 600
      required: [id, label, address]
    PilotQuoteWorkspace:
      type: object
      additionalProperties: false
      properties:
        quote:
          $ref: "#/components/schemas/PilotQuoteAggregate"
        version:
          $ref: "#/components/schemas/PilotQuoteVersion"
        request:
          $ref: "#/components/schemas/PilotQuoteRequestSummary"
        customer:
          $ref: "#/components/schemas/PilotQuoteCustomerSummary"
        location:
          $ref: "#/components/schemas/PilotQuoteLocationSummary"
        replayed:
          type: boolean
      required: [quote, version, request, customer, location]
    PilotQuoteWorkspaceEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/PilotQuoteWorkspace"
      required: [data]
    PilotQuoteLinkResult:
      type: object
      additionalProperties: false
      properties:
        quote:
          $ref: "#/components/schemas/PilotQuoteAggregate"
        version:
          $ref: "#/components/schemas/PilotQuoteVersion"
        request:
          $ref: "#/components/schemas/PilotQuoteRequestSummary"
        customer:
          $ref: "#/components/schemas/PilotQuoteCustomerSummary"
        location:
          $ref: "#/components/schemas/PilotQuoteLocationSummary"
        replayed:
          type: boolean
        publicQuoteUrl:
          type: string
          format: uri
      required: [quote, version, request, customer, location, publicQuoteUrl]
    PilotQuoteLinkEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/PilotQuoteLinkResult"
      required: [data]

    ChangeOrderItemInput:
      type: object
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 300
        specification:
          type: [string, "null"]
          maxLength: 2000
        unit:
          type: string
          minLength: 1
          maxLength: 30
        quantity:
          $ref: "#/components/schemas/Quantity"
        unitPriceMinor:
          $ref: "#/components/schemas/MoneyMinor"
        discountMinor:
          $ref: "#/components/schemas/MoneyMinor"
        taxRate:
          $ref: "#/components/schemas/TaxRate"
        sortOrder:
          type: integer
          minimum: 0
      required: [name, specification, unit, quantity, unitPriceMinor, discountMinor, taxRate, sortOrder]
    ChangeOrderItem:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        name:
          type: string
          maxLength: 300
        specification:
          type: [string, "null"]
          maxLength: 2000
        unit:
          type: string
          maxLength: 30
        quantity:
          $ref: "#/components/schemas/Quantity"
        unitPriceMinor:
          $ref: "#/components/schemas/MoneyMinor"
        discountMinor:
          $ref: "#/components/schemas/MoneyMinor"
        taxRate:
          $ref: "#/components/schemas/TaxRate"
        totalMinor:
          $ref: "#/components/schemas/MoneyMinor"
        sortOrder:
          type: integer
          minimum: 0
      required: [id, name, specification, unit, quantity, unitPriceMinor, discountMinor, taxRate, totalMinor, sortOrder]
    ChangeOrder:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        changeOrderNo:
          type: string
          maxLength: 40
        projectId:
          $ref: "#/components/schemas/Uuid"
        kind:
          $ref: "#/components/schemas/ChangeOrderKind"
        status:
          $ref: "#/components/schemas/ChangeOrderStatus"
        title:
          type: string
          maxLength: 300
        reason:
          type: string
          maxLength: 5000
        scheduleImpactDays:
          type: integer
          minimum: -3650
          maximum: 3650
        items:
          type: array
          maxItems: 300
          items:
            $ref: "#/components/schemas/ChangeOrderItem"
        subtotalMinor:
          $ref: "#/components/schemas/MoneyMinor"
        taxMinor:
          $ref: "#/components/schemas/MoneyMinor"
        totalMinor:
          $ref: "#/components/schemas/MoneyMinor"
        currency:
          $ref: "#/components/schemas/Currency"
        sentAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        respondedAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        lockVersion:
          $ref: "#/components/schemas/LockVersion"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, changeOrderNo, projectId, kind, status, title, reason, scheduleImpactDays, items, subtotalMinor, taxMinor, totalMinor, currency, sentAt, respondedAt, lockVersion, createdAt, updatedAt]
    CreateChangeOrderRequest:
      type: object
      additionalProperties: false
      properties:
        projectId:
          $ref: "#/components/schemas/Uuid"
        kind:
          $ref: "#/components/schemas/ChangeOrderKind"
        title:
          type: string
          minLength: 1
          maxLength: 300
        reason:
          type: string
          minLength: 1
          maxLength: 5000
        scheduleImpactDays:
          type: integer
          minimum: -3650
          maximum: 3650
          default: 0
        currency:
          $ref: "#/components/schemas/Currency"
        items:
          type: array
          minItems: 1
          maxItems: 300
          items:
            $ref: "#/components/schemas/ChangeOrderItemInput"
      required: [projectId, kind, title, reason, scheduleImpactDays, currency, items]
    UpdateChangeOrderRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        kind:
          $ref: "#/components/schemas/ChangeOrderKind"
        title:
          type: string
          minLength: 1
          maxLength: 300
        reason:
          type: string
          minLength: 1
          maxLength: 5000
        scheduleImpactDays:
          type: integer
          minimum: -3650
          maximum: 3650
    ReplaceChangeOrderItemsRequest:
      type: object
      additionalProperties: false
      properties:
        items:
          type: array
          minItems: 1
          maxItems: 300
          items:
            $ref: "#/components/schemas/ChangeOrderItemInput"
      required: [items]
    PublicChangeOrder:
      type: object
      additionalProperties: false
      properties:
        merchant:
          $ref: "#/components/schemas/PublicMerchant"
        changeOrderNo:
          type: string
          maxLength: 40
        kind:
          $ref: "#/components/schemas/ChangeOrderKind"
        status:
          $ref: "#/components/schemas/ChangeOrderStatus"
        title:
          type: string
          maxLength: 300
        reason:
          type: string
          maxLength: 5000
        scheduleImpactDays:
          type: integer
        items:
          type: array
          items:
            $ref: "#/components/schemas/ChangeOrderItem"
        subtotalMinor:
          $ref: "#/components/schemas/MoneyMinor"
        taxMinor:
          $ref: "#/components/schemas/MoneyMinor"
        totalMinor:
          $ref: "#/components/schemas/MoneyMinor"
        currency:
          $ref: "#/components/schemas/Currency"
      required: [merchant, changeOrderNo, kind, status, title, reason, scheduleImpactDays, items, subtotalMinor, taxMinor, totalMinor, currency]
    PublicChangeOrderResponseRequest:
      type: object
      additionalProperties: false
      properties:
        decision:
          $ref: "#/components/schemas/PublicDecision"
        displayName:
          type: string
          minLength: 1
          maxLength: 200
        comment:
          type: [string, "null"]
          maxLength: 2000
      required: [decision, displayName]
    ChangeOrderEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/ChangeOrder"
      required: [data]
    ChangeOrderListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/ChangeOrder"
        meta:
          $ref: "#/components/schemas/ListMeta"
      required: [data, meta]
    PublicChangeOrderEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/PublicChangeOrder"
      required: [data]

    PaymentMilestone:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        projectId:
          $ref: "#/components/schemas/Uuid"
        sequenceNo:
          type: integer
          minimum: 1
        name:
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: [string, "null"]
          maxLength: 2000
        amountMinor:
          $ref: "#/components/schemas/MoneyMinor"
        currency:
          $ref: "#/components/schemas/Currency"
        dueOn:
          anyOf:
            - $ref: "#/components/schemas/BusinessDate"
            - type: "null"
        status:
          $ref: "#/components/schemas/PaymentStatus"
        invoicedAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        paidAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        paymentMethod:
          anyOf:
            - $ref: "#/components/schemas/PaymentMethod"
            - type: "null"
        externalReference:
          type: [string, "null"]
          maxLength: 200
        lockVersion:
          $ref: "#/components/schemas/LockVersion"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, projectId, sequenceNo, name, description, amountMinor, currency, dueOn, status, invoicedAt, paidAt, paymentMethod, externalReference, lockVersion, createdAt, updatedAt]
    CreatePaymentMilestoneRequest:
      type: object
      additionalProperties: false
      properties:
        projectId:
          $ref: "#/components/schemas/Uuid"
        name:
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: [string, "null"]
          maxLength: 2000
        amountMinor:
          $ref: "#/components/schemas/MoneyMinor"
        currency:
          $ref: "#/components/schemas/Currency"
        dueOn:
          anyOf:
            - $ref: "#/components/schemas/BusinessDate"
            - type: "null"
      required: [projectId, name, amountMinor, currency]
    UpdatePaymentMilestoneRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: [string, "null"]
          maxLength: 2000
        amountMinor:
          $ref: "#/components/schemas/MoneyMinor"
        dueOn:
          anyOf:
            - $ref: "#/components/schemas/BusinessDate"
            - type: "null"
    InvoicePaymentMilestoneRequest:
      type: object
      additionalProperties: false
      properties:
        invoicedAt:
          $ref: "#/components/schemas/Timestamp"
    MarkPaymentPaidRequest:
      type: object
      additionalProperties: false
      description: This DTO intentionally has no free-form payment credential fields.
      properties:
        paymentMethod:
          $ref: "#/components/schemas/PaymentMethod"
        paidAt:
          $ref: "#/components/schemas/Timestamp"
        externalReference:
          type: [string, "null"]
          maxLength: 200
        note:
          type: [string, "null"]
          maxLength: 1000
      required: [paymentMethod, paidAt]
    PaymentMilestoneEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/PaymentMilestone"
      required: [data]
    PaymentMilestoneListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/PaymentMilestone"
        meta:
          $ref: "#/components/schemas/ListMeta"
      required: [data, meta]

    MaintenancePlan:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        customerId:
          $ref: "#/components/schemas/Uuid"
        locationId:
          $ref: "#/components/schemas/Uuid"
        assetId:
          type: [string, "null"]
          format: uuid
        serviceCatalogItemId:
          type: [string, "null"]
          format: uuid
        name:
          type: string
          minLength: 1
          maxLength: 200
        cadenceMonths:
          type: integer
          minimum: 1
          maximum: 60
        nextDueOn:
          $ref: "#/components/schemas/BusinessDate"
        lastCompletedWorkOrderId:
          type: [string, "null"]
          format: uuid
        status:
          $ref: "#/components/schemas/MaintenanceStatus"
        leadDays:
          type: integer
          minimum: 0
          maximum: 90
        autoPrepareMessage:
          type: boolean
        lockVersion:
          $ref: "#/components/schemas/LockVersion"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, customerId, locationId, assetId, serviceCatalogItemId, name, cadenceMonths, nextDueOn, lastCompletedWorkOrderId, status, leadDays, autoPrepareMessage, lockVersion, createdAt, updatedAt]
    CreateMaintenancePlanRequest:
      type: object
      additionalProperties: false
      properties:
        customerId:
          $ref: "#/components/schemas/Uuid"
        locationId:
          $ref: "#/components/schemas/Uuid"
        assetId:
          type: [string, "null"]
          format: uuid
        serviceCatalogItemId:
          type: [string, "null"]
          format: uuid
        name:
          type: string
          minLength: 1
          maxLength: 200
        cadenceMonths:
          type: integer
          minimum: 1
          maximum: 60
        nextDueOn:
          $ref: "#/components/schemas/BusinessDate"
        leadDays:
          type: integer
          minimum: 0
          maximum: 90
          default: 14
        autoPrepareMessage:
          type: boolean
          default: true
      required: [customerId, locationId, name, cadenceMonths, nextDueOn, leadDays, autoPrepareMessage]
    UpdateMaintenancePlanRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        assetId:
          type: [string, "null"]
          format: uuid
        serviceCatalogItemId:
          type: [string, "null"]
          format: uuid
        name:
          type: string
          minLength: 1
          maxLength: 200
        cadenceMonths:
          type: integer
          minimum: 1
          maximum: 60
        nextDueOn:
          $ref: "#/components/schemas/BusinessDate"
        leadDays:
          type: integer
          minimum: 0
          maximum: 90
        autoPrepareMessage:
          type: boolean
    PauseMaintenancePlanRequest:
      type: object
      additionalProperties: false
      properties:
        reason:
          type: [string, "null"]
          maxLength: 1000
    ResumeMaintenancePlanRequest:
      type: object
      additionalProperties: false
      properties:
        nextDueOn:
          $ref: "#/components/schemas/BusinessDate"
      required: [nextDueOn]
    CompleteMaintenancePlanRequest:
      type: object
      additionalProperties: false
      properties:
        workOrderId:
          $ref: "#/components/schemas/Uuid"
        completedOn:
          $ref: "#/components/schemas/BusinessDate"
        nextDueOn:
          $ref: "#/components/schemas/BusinessDate"
      required: [workOrderId, completedOn, nextDueOn]
    PrepareMaintenanceRemindersRequest:
      type: object
      additionalProperties: false
      properties:
        dueBefore:
          $ref: "#/components/schemas/BusinessDate"
        limit:
          type: integer
          minimum: 1
          maximum: 100
          default: 100
      required: [dueBefore, limit]
    MaintenancePlanEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/MaintenancePlan"
      required: [data]
    MaintenancePlanListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/MaintenancePlan"
        meta:
          $ref: "#/components/schemas/ListMeta"
      required: [data, meta]
    PreparedNotifications:
      type: object
      additionalProperties: false
      properties:
        prepared:
          type: integer
          minimum: 0
          maximum: 100
        skippedNoConsent:
          type: integer
          minimum: 0
        skippedDuplicate:
          type: integer
          minimum: 0
        notificationIds:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/Uuid"
      required: [prepared, skippedNoConsent, skippedDuplicate, notificationIds]
    PreparedNotificationsEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/PreparedNotifications"
      required: [data]

    NotificationAttempt:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        attemptNo:
          type: integer
          minimum: 1
        startedAt:
          $ref: "#/components/schemas/Timestamp"
        finishedAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        outcome:
          type: string
          enum: [sent, retry, failed]
        providerCode:
          type: [string, "null"]
          maxLength: 100
        errorCategory:
          type: [string, "null"]
          maxLength: 100
      required: [id, attemptNo, startedAt, finishedAt, outcome, providerCode, errorCategory]
    Notification:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        channel:
          $ref: "#/components/schemas/NotificationChannel"
        status:
          $ref: "#/components/schemas/NotificationStatus"
        approvalStatus:
          $ref: "#/components/schemas/NotificationApprovalStatus"
        recipientDisplay:
          type: string
          description: Redacted destination such as LINE…8K2; never a credential.
          maxLength: 200
        subject:
          type: [string, "null"]
          maxLength: 300
        messagePreview:
          type: string
          maxLength: 500
        relatedType:
          type: string
          maxLength: 64
        relatedId:
          $ref: "#/components/schemas/Uuid"
        scheduledAt:
          $ref: "#/components/schemas/Timestamp"
        sentAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        nextAttemptAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        attemptCount:
          type: integer
          minimum: 0
        attempts:
          type: array
          maxItems: 50
          items:
            $ref: "#/components/schemas/NotificationAttempt"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, channel, status, approvalStatus, recipientDisplay, subject, messagePreview, relatedType, relatedId, scheduledAt, sentAt, nextAttemptAt, attemptCount, attempts, createdAt, updatedAt]
    ApproveNotificationsRequest:
      type: object
      additionalProperties: false
      properties:
        notificationIds:
          type: array
          minItems: 1
          maxItems: 100
          uniqueItems: true
          items:
            $ref: "#/components/schemas/Uuid"
      required: [notificationIds]
    CancelNotificationRequest:
      type: object
      additionalProperties: false
      properties:
        reason:
          type: [string, "null"]
          maxLength: 1000
    NotificationBatch:
      type: object
      additionalProperties: false
      properties:
        approved:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/Uuid"
        rejected:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/NotificationBatchRejection"
      required: [approved, rejected]
    NotificationBatchRejection:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
          pattern: "^[A-Z][A-Z0-9_]*$"
      required: [id, code]
    NotificationEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/Notification"
      required: [data]
    NotificationListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Notification"
        meta:
          $ref: "#/components/schemas/ListMeta"
      required: [data, meta]
    NotificationBatchEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/NotificationBatch"
      required: [data]

    LineChannel:
      type: object
      additionalProperties: false
      description: Safe metadata only; no secret, token, ciphertext or secret-length hint.
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        name:
          type: string
          minLength: 1
          maxLength: 200
        channelId:
          type: string
          minLength: 1
          maxLength: 100
        liffId:
          type: [string, "null"]
          maxLength: 100
        status:
          $ref: "#/components/schemas/LineChannelStatus"
        credentialConfigured:
          type: boolean
        credentialVersion:
          type: integer
          minimum: 1
        webhookUrl:
          type: string
          format: uri
        verifiedAt:
          anyOf:
            - $ref: "#/components/schemas/Timestamp"
            - type: "null"
        lockVersion:
          $ref: "#/components/schemas/LockVersion"
        createdAt:
          $ref: "#/components/schemas/Timestamp"
        updatedAt:
          $ref: "#/components/schemas/Timestamp"
      required: [id, name, channelId, liffId, status, credentialConfigured, credentialVersion, webhookUrl, verifiedAt, lockVersion, createdAt, updatedAt]
    CreateLineChannelRequest:
      type: object
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        channelId:
          type: string
          minLength: 1
          maxLength: 100
        channelSecret:
          type: string
          writeOnly: true
          minLength: 16
          maxLength: 512
        channelAccessToken:
          type: string
          writeOnly: true
          minLength: 32
          maxLength: 4096
        liffId:
          type: [string, "null"]
          maxLength: 100
      required: [name, channelId, channelSecret, channelAccessToken]
    UpdateLineChannelRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        liffId:
          type: [string, "null"]
          maxLength: 100
    RotateLineTokenRequest:
      type: object
      additionalProperties: false
      properties:
        channelAccessToken:
          type: string
          writeOnly: true
          minLength: 32
          maxLength: 4096
      required: [channelAccessToken]
    DisableLineChannelRequest:
      type: object
      additionalProperties: false
      properties:
        reason:
          type: [string, "null"]
          maxLength: 1000
    LineChannelEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/LineChannel"
      required: [data]
    LineChannelListEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/LineChannel"
      required: [data]
    LineWebhookPayload:
      type: object
      description: |
        Provider-owned JSON verified from raw bytes before decoding. Unknown provider
        fields are intentionally accepted for forward compatibility, while the server
        separately caps body bytes and event count.
      additionalProperties: true
      properties:
        destination:
          type: string
          minLength: 1
          maxLength: 200
        events:
          type: array
          maxItems: 100
          items:
            type: object
            description: Provider event persisted as an encrypted/redacted inbox payload.
            additionalProperties: true
      required: [destination, events]
    WebhookAcknowledgement:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
          const: true
      required: [ok]

    MetricWindow:
      type: object
      additionalProperties: false
      properties:
        from:
          $ref: "#/components/schemas/BusinessDate"
        to:
          $ref: "#/components/schemas/BusinessDate"
        timezone:
          type: string
          maxLength: 64
      required: [from, to, timezone]
    RatioMetric:
      type: object
      additionalProperties: false
      properties:
        numerator:
          type: integer
          minimum: 0
        denominator:
          type: integer
          minimum: 0
        ratio:
          type: [number, "null"]
          minimum: 0
          maximum: 1
        window:
          $ref: "#/components/schemas/MetricWindow"
      required: [numerator, denominator, ratio, window]
    DurationMetric:
      type: object
      additionalProperties: false
      properties:
        medianSeconds:
          type: [integer, "null"]
          minimum: 0
        p90Seconds:
          type: [integer, "null"]
          minimum: 0
        sampleSize:
          type: integer
          minimum: 0
        window:
          $ref: "#/components/schemas/MetricWindow"
      required: [medianSeconds, p90Seconds, sampleSize, window]
    DashboardQueue:
      type: object
      additionalProperties: false
      properties:
        newServiceRequests:
          type: integer
          minimum: 0
        quotesAwaitingApproval:
          type: integer
          minimum: 0
        unassignedWorkOrders:
          type: integer
          minimum: 0
        overduePayments:
          type: integer
          minimum: 0
        maintenanceDue:
          type: integer
          minimum: 0
        notificationsFailed:
          type: integer
          minimum: 0
      required: [newServiceRequests, quotesAwaitingApproval, unassignedWorkOrders, overduePayments, maintenanceDue, notificationsFailed]
    Dashboard:
      type: object
      additionalProperties: false
      properties:
        responseSpeed:
          $ref: "#/components/schemas/DurationMetric"
        quoteAcceptanceRate:
          $ref: "#/components/schemas/RatioMetric"
        completionRate:
          $ref: "#/components/schemas/RatioMetric"
        retentionRate:
          $ref: "#/components/schemas/RatioMetric"
        queues:
          $ref: "#/components/schemas/DashboardQueue"
      required: [responseSpeed, quoteAcceptanceRate, completionRate, retentionRate, queues]
    DashboardEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/Dashboard"
      required: [data]
    FunnelStage:
      type: object
      additionalProperties: false
      properties:
        stage:
          type: string
          enum: [request, quote_sent, quote_accepted, work_completed]
        count:
          type: integer
          minimum: 0
        conversionFromPrevious:
          type: [number, "null"]
          minimum: 0
          maximum: 1
      required: [stage, count, conversionFromPrevious]
    FunnelReport:
      type: object
      additionalProperties: false
      properties:
        window:
          $ref: "#/components/schemas/MetricWindow"
        stages:
          type: array
          minItems: 4
          maxItems: 4
          items:
            $ref: "#/components/schemas/FunnelStage"
      required: [window, stages]
    FunnelReportEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/FunnelReport"
      required: [data]
    TechnicianLoadBucket:
      type: object
      additionalProperties: false
      properties:
        date:
          $ref: "#/components/schemas/BusinessDate"
        assignedHours:
          type: number
          minimum: 0
        activeTechnicians:
          type: integer
          minimum: 0
      required: [date, assignedHours, activeTechnicians]
    OperationsReport:
      type: object
      additionalProperties: false
      properties:
        responseSpeed:
          $ref: "#/components/schemas/DurationMetric"
        completionRate:
          $ref: "#/components/schemas/RatioMetric"
        aggregateTechnicianLoad:
          type: array
          maxItems: 366
          items:
            $ref: "#/components/schemas/TechnicianLoadBucket"
      required: [responseSpeed, completionRate, aggregateTechnicianLoad]
    OperationsReportEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/OperationsReport"
      required: [data]
    RetentionReport:
      type: object
      additionalProperties: false
      properties:
        window:
          $ref: "#/components/schemas/MetricWindow"
        maintenanceDue:
          type: integer
          minimum: 0
        remindersSent:
          type: integer
          minimum: 0
        reminderBookingConversion:
          $ref: "#/components/schemas/RatioMetric"
      required: [window, maintenanceDue, remindersSent, reminderBookingConversion]
    RetentionReportEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/RetentionReport"
      required: [data]

    WorkerBatchRequest:
      type: object
      additionalProperties: false
      properties:
        limit:
          type: integer
          minimum: 1
          maximum: 50
      required: [limit]
    WorkerResult:
      type: object
      additionalProperties: false
      properties:
        claimed:
          type: integer
          minimum: 0
        succeeded:
          type: integer
          minimum: 0
        retried:
          type: integer
          minimum: 0
        failed:
          type: integer
          minimum: 0
      required: [claimed, succeeded, retried, failed]
    WorkerResultEnvelope:
      type: object
      additionalProperties: false
      properties:
        data:
          $ref: "#/components/schemas/WorkerResult"
      required: [data]
